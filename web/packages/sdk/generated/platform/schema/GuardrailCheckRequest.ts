/**
 * SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by Orval 🍺
 * Do not edit manually.
 * Nemo Platform API
 */
import type { ChatCompletionAssistantMessageParam } from './ChatCompletionAssistantMessageParam';
import type { ChatCompletionFunctionMessageParam } from './ChatCompletionFunctionMessageParam';
import type { ChatCompletionSystemMessageParam } from './ChatCompletionSystemMessageParam';
import type { ChatCompletionToolMessageParam } from './ChatCompletionToolMessageParam';
import type { ChatCompletionUserMessageParam } from './ChatCompletionUserMessageParam';
import type { GuardrailCheckRequestFunctionCall } from './GuardrailCheckRequestFunctionCall';
import type { GuardrailCheckRequestLogitBias } from './GuardrailCheckRequestLogitBias';
import type { GuardrailCheckRequestResponseFormat } from './GuardrailCheckRequestResponseFormat';
import type { GuardrailCheckRequestStreamOptions } from './GuardrailCheckRequestStreamOptions';
import type { GuardrailCheckRequestToolChoice } from './GuardrailCheckRequestToolChoice';
import type { GuardrailCheckRequestToolsItem } from './GuardrailCheckRequestToolsItem';
import type { GuardrailsDataInput } from './GuardrailsDataInput';

/**
 * Currently only inherits, in the future we might add new fields.
 */
export interface GuardrailCheckRequest {
  /** The model to use for completion. Must be one of the available models. */
  model: string;
  /** Format of the response. Use {'type': 'json_object'} for JSON mode or {'type': 'json_schema', 'json_schema': {...}} for structured outputs. */
  response_format?: GuardrailCheckRequestResponseFormat;
  /**
   * The maximum number of tokens that can be generated in the chat completion.
   * @minimum 1
   */
  max_tokens?: number;
  /**
   * How many chat completion choices to generate for each input message.
   * @minimum 1
   */
  n?: number;
  /** If set, partial message deltas will be sent, like in ChatGPT. */
  stream?: boolean;
  /**
   * What sampling temperature to use, between 0 and 2.
   * @minimum 0
   * @maximum 2
   */
  temperature?: number;
  /**
   * An alternative to sampling with temperature, called nucleus sampling.
   * @minimum 0
   * @maximum 1
   */
  top_p?: number;
  /** Up to 4 sequences where the API will stop generating further tokens. */
  stop?: string | string[];
  /**
   * Positive values penalize new tokens based on their existing frequency in the text.
   * @minimum -2
   * @maximum 2
   */
  frequency_penalty?: number;
  /**
   * Positive values penalize new tokens based on whether they appear in the text so far.
   * @minimum -2
   * @maximum 2
   */
  presence_penalty?: number;
  /** Deprecated in favor of tool_choice. 'none' means the model will not call a function and instead generates a message. 'auto' means the model can pick between generating a message or calling a function. Specifying a particular function via {'name': 'my_function'} forces the model to call that function. */
  function_call?: GuardrailCheckRequestFunctionCall;
  /** If specified, attempts to sample deterministically. */
  seed?: number;
  /** Modify the likelihood of specified tokens appearing in the completion. Maps token IDs (as strings) to bias values from -100 to 100. */
  logit_bias?: GuardrailCheckRequestLogitBias;
  /**
   * The number of most likely tokens to return at each token position.
   * @minimum 0
   * @maximum 20
   */
  top_logprobs?: number;
  /** Whether to return log probabilities of the output tokens or not. If true, returns the log probabilities of each output token returned in the content of message */
  logprobs?: boolean;
  /** Controls which (if any) tool is called by the model. 'none' means no tool is called, 'auto' lets the model decide, 'required' forces a tool call. */
  tool_choice?: GuardrailCheckRequestToolChoice;
  /** A unique identifier representing your end-user, used by some providers for abuse monitoring. */
  user?: string;
  /** A list of tools the model may call. Each tool is an object with a 'type' field and a 'function' definition. */
  tools?: GuardrailCheckRequestToolsItem[];
  /** Ignore the eos when running */
  ignore_eos?: boolean;
  /** Constrains effort on reasoning for reasoning models. Reducing reasoning effort can result in faster responses and fewer tokens used on reasoning in a response. */
  reasoning_effort?: string;
  /**
   * An upper bound for the number of tokens that can be generated for a completion, including visible output tokens and reasoning tokens. Preferred over max_tokens for reasoning models.
   * @minimum 1
   */
  max_completion_tokens?: number;
  /** Options for streaming response. Only set this when stream=True. Supports include_usage to receive token usage in the final stream chunk. */
  stream_options?: GuardrailCheckRequestStreamOptions;
  /** A list of messages comprising the conversation so far */
  messages: (
    | ChatCompletionSystemMessageParam
    | ChatCompletionUserMessageParam
    | ChatCompletionAssistantMessageParam
    | ChatCompletionToolMessageParam
    | ChatCompletionFunctionMessageParam
  )[];
  /** Whether this is a vision-capable request with image inputs. */
  vision?: boolean;
  /** Guardrails specific options for the request. */
  guardrails?: GuardrailsDataInput;
  [key: string]: unknown;
}
