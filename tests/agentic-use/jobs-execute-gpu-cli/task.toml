version = "1.0"

[metadata]
author_name = "NeMo Platform Team"
difficulty = "medium"
category = "cli-jobs"
tags = ["jobs", "execute", "gpu", "docker", "cli", "cuda"]

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 900.0

[environment]
build_timeout_sec = 600.0
cpus = 4
memory_mb = 16384
storage_mb = 30720
# GPU access provided via docker-compose.yaml (Harbor Docker env doesn't support gpus natively)
gpus = 0
allow_internet = true

[verifier.env]
