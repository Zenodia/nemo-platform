/**
 * SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by Orval 🍺
 * Do not edit manually.
 * Nemo Platform API
 */
import type { BenchmarkOnlineJobPromptTemplate } from './BenchmarkOnlineJobPromptTemplate';
import type { BenchmarkRef } from './BenchmarkRef';
import type { EvaluatorModel } from './EvaluatorModel';
import type { ModelRef } from './ModelRef';
import type { RunConfigOnlineModel } from './RunConfigOnlineModel';

/**
 * Input for an online benchmark evaluation job.

Evaluates a model by prompting it with the benchmark's dataset and then evaluating
the responses against all metrics in the benchmark.
 */
export interface BenchmarkOnlineJob {
  /** Reference to the benchmark for evaluation (format: workspace/name). */
  benchmark: BenchmarkRef;
  /** The model to evaluate. */
  model: EvaluatorModel | ModelRef;
  /** Execution parameters for the benchmark job. */
  params?: RunConfigOnlineModel;
  /** The jinja template to prompt the model for evaluation. Can be either a simple string or a structured object (e.g., OpenAI messages format). Use Jinja template variables like {{input}}, {{output}}, {{context}}, {{reference}} to reference input columns. */
  prompt_template: BenchmarkOnlineJobPromptTemplate;
  /** Prompt template fields that should remain available to the prompt template but not be required by dataset schema validation. */
  optional_fields?: string[];
}
