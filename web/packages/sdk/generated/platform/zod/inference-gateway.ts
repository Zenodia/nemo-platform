/**
 * SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by Orval 🍺
 * Do not edit manually.
 * Nemo Platform API
 */
import * as zod from 'zod';

/**
 * Proxy requests to model entity inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary Model Inference Proxy PATCH
 */
export const GatewayProxyPatchParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const GatewayProxyPatchBody = zod.record(zod.string(), zod.unknown());

export const GatewayProxyPatchResponse = zod.record(zod.string(), zod.unknown());

/**
 * Proxy requests to model entity inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary Model Inference Proxy DELETE
 */
export const GatewayProxyDeleteParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const GatewayProxyDeleteResponse = zod.unknown();

/**
 * Proxy requests to model entity inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary Model Inference Proxy PUT
 */
export const GatewayProxyPutParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const GatewayProxyPutBody = zod.record(zod.string(), zod.unknown());

export const GatewayProxyPutResponse = zod.record(zod.string(), zod.unknown());

/**
 * Proxy requests to model entity inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary Model Inference Proxy POST
 */
export const GatewayProxyPostParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const GatewayProxyPostBody = zod.record(zod.string(), zod.unknown());

export const GatewayProxyPostResponse = zod.record(zod.string(), zod.unknown());

/**
 * Proxy requests to model entity inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary Model Inference Proxy GET
 */
export const GatewayProxyGetParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const GatewayProxyGetResponse = zod.unknown();

/**
 * This endpoint aggregates models from all model entities and returns them
in OpenAI's list models format. Each model ID is the model entity identifier
in format workspace/model_entity_name.
 * @summary OpenAI List Models
 */
export const OpenaiProxyListModelsParams = zod.object({
  workspace: zod.string(),
});

export const openaiProxyListModelsResponseDataItemObjectDefault = `model`;
export const openaiProxyListModelsResponseDataItemCreatedDefault = 0;
export const openaiProxyListModelsResponseObjectDefault = `list`;

export const OpenaiProxyListModelsResponse = zod
  .object({
    data: zod.array(
      zod
        .object({
          id: zod.string(),
          owned_by: zod.string(),
          object: zod.string().default(openaiProxyListModelsResponseDataItemObjectDefault),
          created: zod.number().default(openaiProxyListModelsResponseDataItemCreatedDefault),
        })
        .describe('Duplicated structure for an OpenAI \/v1\/models individual model response.')
    ),
    object: zod.string().default(openaiProxyListModelsResponseObjectDefault),
  })
  .describe('Duplicated structure for an OpenAI \/v1\/models response.');

/**
 * Retrieve information about a specific OpenAI-compatible model.
Workspace is always taken from the URL path; name may be model_entity_name
or workspace/model_entity_name (workspace prefix is ignored).
 * @summary OpenAI Get Model
 */
export const OpenaiProxyGetModelParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

export const openaiProxyGetModelResponseObjectDefault = `model`;
export const openaiProxyGetModelResponseCreatedDefault = 0;

export const OpenaiProxyGetModelResponse = zod
  .object({
    id: zod.string(),
    owned_by: zod.string(),
    object: zod.string().default(openaiProxyGetModelResponseObjectDefault),
    created: zod.number().default(openaiProxyGetModelResponseCreatedDefault),
  })
  .describe('Duplicated structure for an OpenAI \/v1\/models individual model response.');

/**
 * Proxy requests to OpenAI-compatible inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary OpenAI Inference Proxy PATCH
 */
export const OpenaiProxyPatchParams = zod.object({
  workspace: zod.string(),
  trailing_uri: zod.string(),
});

export const OpenaiProxyPatchBody = zod.record(zod.string(), zod.unknown());

export const OpenaiProxyPatchResponse = zod.record(zod.string(), zod.unknown());

/**
 * Proxy requests to OpenAI-compatible inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary OpenAI Inference Proxy DELETE
 */
export const OpenaiProxyDeleteParams = zod.object({
  workspace: zod.string(),
  trailing_uri: zod.string(),
});

export const OpenaiProxyDeleteResponse = zod.unknown();

/**
 * Proxy requests to OpenAI-compatible inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary OpenAI Inference Proxy PUT
 */
export const OpenaiProxyPutParams = zod.object({
  workspace: zod.string(),
  trailing_uri: zod.string(),
});

export const OpenaiProxyPutBody = zod.record(zod.string(), zod.unknown());

export const OpenaiProxyPutResponse = zod.record(zod.string(), zod.unknown());

/**
 * Proxy requests to OpenAI-compatible inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary OpenAI Inference Proxy POST
 */
export const OpenaiProxyPostParams = zod.object({
  workspace: zod.string(),
  trailing_uri: zod.string(),
});

export const OpenaiProxyPostBody = zod.record(zod.string(), zod.unknown());

export const OpenaiProxyPostResponse = zod.record(zod.string(), zod.unknown());

/**
 * Proxy requests to OpenAI-compatible inference endpoints.

All inference requests must resolve to a `VirtualModel`. The platform's
provider reconciler auto-creates an implicit `autoprovisioned` VirtualModel
for every served model entity (named after the entity, with
`default_model_entity` set to the entity ref) so this is the typical case;
operators can also create custom VirtualModels for routing, plugin chains,
LoRA escape-hatches, etc. Requests for which no VirtualModel can be found
return `404`.
 * @summary OpenAI Inference Proxy GET
 */
export const OpenaiProxyGetParams = zod.object({
  workspace: zod.string(),
  trailing_uri: zod.string(),
});

export const OpenaiProxyGetResponse = zod.unknown();

/**
 * Proxy requests to provider inference endpoints.
 * @summary Provider Inference Proxy PATCH
 */
export const ProviderProxyPatchParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const ProviderProxyPatchBody = zod.record(zod.string(), zod.unknown());

export const ProviderProxyPatchResponse = zod.record(zod.string(), zod.unknown());

/**
 * Proxy requests to provider inference endpoints.
 * @summary Provider Inference Proxy DELETE
 */
export const ProviderProxyDeleteParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const ProviderProxyDeleteResponse = zod.unknown();

/**
 * Proxy requests to provider inference endpoints.
 * @summary Provider Inference Proxy PUT
 */
export const ProviderProxyPutParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const ProviderProxyPutBody = zod.record(zod.string(), zod.unknown());

export const ProviderProxyPutResponse = zod.record(zod.string(), zod.unknown());

/**
 * Proxy requests to provider inference endpoints.
 * @summary Provider Inference Proxy POST
 */
export const ProviderProxyPostParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const ProviderProxyPostBody = zod.record(zod.string(), zod.unknown());

export const ProviderProxyPostResponse = zod.record(zod.string(), zod.unknown());

/**
 * Proxy requests to provider inference endpoints.
 * @summary Provider Inference Proxy GET
 */
export const ProviderProxyGetParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
  trailing_uri: zod.string(),
});

export const ProviderProxyGetResponse = zod.unknown();

/**
 * Check if a model provider is registered in the gateway's cache.

This is a lightweight endpoint that only checks the gateway's internal state,
without making any requests to the actual provider backend. Use this to verify
the gateway is ready to route requests to a provider after deployment.

Returns:
    200 OK with provider info if the provider is registered
    404 Not Found if the provider is not yet in the gateway's cache
 * @summary Check Provider Readiness
 */
export const ProviderReadyParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

export const ProviderReadyResponse = zod.record(zod.string(), zod.unknown());
