/**
 * SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by Orval 🍺
 * Do not edit manually.
 * Nemo Platform API
 */
import * as zod from 'zod';

/**
 * List all entries with filtering capabilities.

When longest_per_thread=true is set in filters, returns only the longest entry
(by message count) for each unique thread_id.
 * @summary List Entries
 */
export const ListEntriesParams = zod.object({
  workspace: zod.string(),
});

export const listEntriesQueryPageDefault = 1;
export const listEntriesQueryPageSizeDefault = 10;
export const listEntriesQuerySortDefault = `created_at`;

export const ListEntriesQueryParams = zod.object({
  page: zod.number().default(listEntriesQueryPageDefault).describe('Page number.'),
  page_size: zod.number().default(listEntriesQueryPageSizeDefault).describe('Page size.'),
  sort: zod
    .enum(['created_at', '-created_at', 'updated_at', '-updated_at'])
    .describe('Sort fields for Entries.')
    .default(listEntriesQuerySortDefault)
    .describe(
      'The field to sort by. To sort in decreasing order, use `-` in front of the field name.'
    ),
  filter: zod
    .object({
      id: zod
        .union([zod.string(), zod.record(zod.string(), zod.unknown())])
        .optional()
        .describe(
          "Filter by entry ID. Supports operators like {'in': ['entry-ABC', 'entry-XYZ']} for multiple IDs."
        ),
      workspace: zod.string().optional().describe('Filter by workspace id.'),
      project: zod.string().optional().describe('Filter by project name.'),
      external_id: zod
        .union([zod.string(), zod.record(zod.string(), zod.unknown())])
        .optional()
        .describe(
          "Filter by external ID. Supports operators like {'in': ['id1', 'id2']} for multiple IDs."
        ),
      context: zod
        .object({
          app: zod.string().optional().describe('Filter by app reference (workspace\/name).'),
          task: zod.string().optional().describe('Filter by task reference.'),
          thread_id: zod.string().optional().describe('Filter by thread ID.'),
          user_id: zod.string().optional().describe('Filter by user ID.'),
          session_id: zod.string().optional().describe('Filter by session ID.'),
        })
        .describe('Filter for entry context fields.')
        .optional()
        .describe('Filter by context fields.'),
      user_rating: zod
        .object({
          thumb: zod
            .enum(['up', 'down'])
            .describe('Possible thumb feedback choices.')
            .optional()
            .describe('Filter by thumb direction.'),
        })
        .describe('Filter for entry user rating fields.')
        .optional()
        .describe('Filter by user rating fields.'),
      model: zod
        .string()
        .optional()
        .describe(
          "Filter by the served model recorded in usage (e.g., 'gpt-4o', 'meta\/llama-3.1-70b-instruct')."
        ),
      has_thumb: zod.boolean().optional().describe('Filter by presence of thumb feedback.'),
      has_rating: zod.boolean().optional().describe('Filter by presence of rating.'),
      has_opinion: zod.boolean().optional().describe('Filter by presence of opinion.'),
      has_rewrite: zod.boolean().optional().describe('Filter by presence of rewrite.'),
      has_events: zod.boolean().optional().describe('Filter by presence of any events.'),
      longest_per_thread: zod
        .boolean()
        .optional()
        .describe('If true, return only the longest entry per thread (based on message count).'),
      created_at: zod
        .object({
          $gte: zod
            .string()
            .datetime({})
            .optional()
            .describe('Filter for results greater than or equal to this datetime.'),
          $lte: zod
            .string()
            .datetime({})
            .optional()
            .describe('Filter for results less than or equal to this datetime.'),
        })
        .optional()
        .describe('Filter entities based on creation date.'),
      updated_at: zod
        .object({
          $gte: zod
            .string()
            .datetime({})
            .optional()
            .describe('Filter for results greater than or equal to this datetime.'),
          $lte: zod
            .string()
            .datetime({})
            .optional()
            .describe('Filter for results less than or equal to this datetime.'),
        })
        .optional()
        .describe('Filter entities based on update date.'),
    })
    .optional()
    .describe(
      'Filter entries by id, project, external_id, created_at, updated_at, usage fields (model), context fields, and user_rating fields.'
    ),
});

export const listEntriesResponseDataItemUsageOneLatencyMsMin = 0;

export const listEntriesResponseDataItemUsageOneCostUsdMin = 0;

export const listEntriesResponseDataItemUsageOneCostInputUsdMin = 0;

export const listEntriesResponseDataItemUsageOneCostOutputUsdMin = 0;

export const listEntriesResponseDataItemUsageOneInputTokensMin = 0;

export const listEntriesResponseDataItemUsageOneOutputTokensMin = 0;

export const listEntriesResponseDataItemUsageOneCachedTokensMin = 0;

export const listEntriesResponseDataItemUserRatingOneRatingMin = 0;

export const listEntriesResponseDataItemUserRatingOneOpinionMax = 2000;

export const listEntriesResponseDataItemUserRatingOneRewriteMax = 10000;

export const listEntriesResponseDataItemUserRatingOneChosenIndexMin = 0;

export const listEntriesResponseDataItemEventsItemOneEventTypeDefault = `user_feedback`;
export const listEntriesResponseDataItemEventsItemOneRatingMin = 0;

export const listEntriesResponseDataItemEventsItemOneOpinionMax = 2000;

export const listEntriesResponseDataItemEventsItemOneRewriteMax = 10000;

export const listEntriesResponseDataItemEventsItemOneChosenIndexMin = 0;

export const listEntriesResponseDataItemEventsItemTwoEventTypeDefault = `user_action`;
export const listEntriesResponseDataItemEventsItemTwoActionMax = 256;

export const listEntriesResponseDataItemEventsItemTwoMetadataOneMax = 256;

export const listEntriesResponseDataItemEventsItemTwoMetadataTwoItemMax = 256;

export const listEntriesResponseDataItemEventsItemThreeEventTypeDefault = `reviewer_annotation`;
export const listEntriesResponseDataItemEventsItemThreeRatingMin = 0;

export const listEntriesResponseDataItemEventsItemThreeOpinionMax = 2000;

export const listEntriesResponseDataItemEventsItemThreeRewriteMax = 10000;

export const listEntriesResponseDataItemEventsItemThreeChosenIndexMin = 0;

export const listEntriesResponseDataItemEventsItemFourEventTypeDefault = `evaluator_result`;
export const listEntriesResponseDataItemEventsItemFourNameMax = 256;

export const ListEntriesResponse = zod.object({
  data: zod.array(
    zod
      .object({
        id: zod.string().describe('Unique identifier'),
        name: zod.string().describe('Entry name (auto-generated)'),
        workspace: zod.string().describe('Workspace identifier'),
        external_id: zod.string().optional().describe('Client-provided identifier'),
        project: zod
          .string()
          .optional()
          .describe('The name of the project associated with this entry'),
        data: zod
          .object({
            request: zod
              .object({
                messages: zod
                  .array(
                    zod
                      .object({
                        role: zod
                          .enum(['user', 'system', 'assistant', 'developer', 'tool', 'function'])
                          .describe('Valid role values for entry request messages.')
                          .describe(
                            'The role of the message sender. Must be one of: user, system, assistant, developer, tool, or function.'
                          ),
                      })
                      .describe(
                        'A flexible message model that requires a valid role field but allows any other fields.\n\nThis flexibility enables the Intake service to store messages from various LLM providers\nand future model types without requiring schema updates. Additional fields like `content`,\n`name`, `tool_calls`, `tool_call_id`, etc. are all accepted.\n\nExamples of additional fields:\n- `content`: The message text or content\n- `name`: Name of the user or function\n- `tool_calls`: Tool\/function calls in the message\n- `tool_call_id`: ID of the tool call being responded to'
                      )
                  )
                  .describe(
                    'A list of messages comprising the conversation. Each message must have a valid role. Additional fields like `content`, `tool_calls`, etc. are provider-specific.'
                  ),
                model: zod
                  .string()
                  .describe(
                    "The model identifier used for this request (e.g., 'gpt-4', 'llama-3-70b', 'claude-3-opus')."
                  ),
              })
              .describe(
                'Flexible entry request that accepts any object shape.\n\nThis flexibility enables the Intake service to store requests from various LLM providers\n(OpenAI, Anthropic, NIM, etc.) and future model types (embeddings, multimodal, etc.)\nwithout requiring schema updates.\n\nRequired fields: `messages` and `model`\nCommon optional fields: `temperature`, `max_tokens`, `top_p`, `tools`, `tool_choice`,\n`stream`, `response_format`, etc.'
              )
              .describe('Raw request payload recorded from the client.'),
            response: zod
              .union([zod.unknown(), zod.unknown()])
              .and(
                zod.object({
                  choices: zod
                    .array(zod.record(zod.string(), zod.unknown()))
                    .optional()
                    .describe(
                      'A list of response choices generated by the model. Each choice typically contains a message object with role, content, and optional tool_calls. The structure varies by provider but commonly includes fields like `index`, `message`, and `finish_reason`.'
                    ),
                  error: zod
                    .record(zod.string(), zod.unknown())
                    .optional()
                    .describe(
                      "Error envelope returned by the provider when the call failed (e.g., OpenAI's `{message, type, code, param}` shape). Mutually exclusive with `choices`: a response carries one or the other."
                    ),
                })
              )
              .describe(
                'Flexible entry response that accepts any object shape.\n\nThis flexibility enables the Intake service to store responses from various LLM providers\nand future model types without requiring schema updates.\n\nRequired: either `choices` (successful response) or `error` (failed call).\nCommon optional fields: `id`, `created`, `model`, `usage`, `system_fingerprint`, etc.'
              )
              .describe('Raw response payload generated by the model.'),
          })
          .describe('Entry data containing the request and response for an LLM interaction.')
          .describe('Entry data containing request and response'),
        usage: zod
          .object({
            model: zod
              .string()
              .optional()
              .describe(
                'The actual model that served the request (after any routing). May differ from the model in the request body.'
              ),
            started_at: zod
              .string()
              .datetime({})
              .optional()
              .describe('UTC timestamp when the upstream LLM call started.'),
            ended_at: zod
              .string()
              .datetime({})
              .optional()
              .describe('UTC timestamp when the upstream LLM call ended.'),
            latency_ms: zod
              .number()
              .min(listEntriesResponseDataItemUsageOneLatencyMsMin)
              .optional()
              .describe('Wall-clock latency of the upstream LLM call, in milliseconds.'),
            cost_usd: zod
              .number()
              .min(listEntriesResponseDataItemUsageOneCostUsdMin)
              .optional()
              .describe('Total estimated cost of this call, in USD.'),
            cost_input_usd: zod
              .number()
              .min(listEntriesResponseDataItemUsageOneCostInputUsdMin)
              .optional()
              .describe('Estimated cost attributed to input tokens, in USD.'),
            cost_output_usd: zod
              .number()
              .min(listEntriesResponseDataItemUsageOneCostOutputUsdMin)
              .optional()
              .describe('Estimated cost attributed to output tokens, in USD.'),
            input_tokens: zod
              .number()
              .min(listEntriesResponseDataItemUsageOneInputTokensMin)
              .optional()
              .describe('Number of input tokens consumed.'),
            output_tokens: zod
              .number()
              .min(listEntriesResponseDataItemUsageOneOutputTokensMin)
              .optional()
              .describe('Number of output tokens produced.'),
            cached_tokens: zod
              .number()
              .min(listEntriesResponseDataItemUsageOneCachedTokensMin)
              .optional()
              .describe(
                'Number of input tokens served from a prompt cache (subset of input_tokens).'
              ),
          })
          .describe(
            'Structured usage metrics captured at log time.\n\nEvery field is optional so producers can populate whatever they have without\nschema breakage. Stored as the entry-level ``usage`` field so filters can\nreach it via ``data.usage.<field>`` entity-store paths.'
          )
          .optional()
          .describe('Structured usage metrics (model served, latency, cost, token counts).'),
        context: zod
          .object({
            app: zod
              .string()
              .describe(
                "Reference to the app that produced this entry, in the form `workspace\/name`. If the app doesn't exist, it will be automatically created when the entry is ingested."
              ),
            task: zod
              .string()
              .describe(
                "Name of the task within the app (e.g., 'chat', 'completion', 'tool-call'). If the task doesn't exist, it will be automatically created when the entry is ingested."
              ),
            thread_id: zod
              .string()
              .optional()
              .describe(
                'Logical thread identifier that groups related entries in a multi-turn conversation. If provided, entries with the same thread_id are treated as part of the same conversation. If omitted, the entry is treated as a single-turn interaction (e.g., a standalone tool call or completion).'
              ),
            user_id: zod
              .string()
              .optional()
              .describe(
                "Identifier of the application's end-user who triggered this LLM interaction. This represents the person using your application (e.g., 'customer_123', 'employee@company.com'), NOT the service account that created the entry record (see ownership.created_by for that). Use this to track which of your users a conversation belongs to, filter entries by user, and enable per-user analytics. Format is application-defined."
              ),
            trace_id: zod
              .string()
              .optional()
              .describe(
                'Distributed trace identifier (e.g., W3C traceparent). Intake stores it verbatim and does not use it at ingestion time; helps with later cross-system joins.'
              ),
            session_id: zod
              .string()
              .optional()
              .describe(
                'Long-lived session identifier (e.g., user account or browser session). Stored for post-processing analytics; not used by the Intake service at runtime.'
              ),
            created_at: zod
              .string()
              .datetime({})
              .optional()
              .describe('UTC timestamp when the entry was created.'),
          })
          .describe(
            'Contextual metadata attached to every entry record.\n\nKeeping these grouped in a dedicated object avoids polluting the top-level\nentity schema and makes it trivial to extend without breaking compatibility.'
          )
          .describe('Metadata describing producer, task, trace'),
        user_rating: zod
          .object({
            thumb: zod
              .enum(['up', 'down'])
              .describe('Possible thumb feedback choices.')
              .optional()
              .describe(
                'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
              ),
            rating: zod
              .number()
              .min(listEntriesResponseDataItemUserRatingOneRatingMin)
              .optional()
              .describe(
                'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
              ),
            opinion: zod
              .string()
              .min(1)
              .max(listEntriesResponseDataItemUserRatingOneOpinionMax)
              .optional()
              .describe(
                'Free-text comment from the end user describing their opinion of the response.'
              ),
            rewrite: zod
              .string()
              .min(1)
              .max(listEntriesResponseDataItemUserRatingOneRewriteMax)
              .optional()
              .describe(
                "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
              ),
            chosen_index: zod
              .number()
              .min(listEntriesResponseDataItemUserRatingOneChosenIndexMin)
              .optional()
              .describe(
                'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
              ),
            categories: zod
              .record(zod.string(), zod.union([zod.number(), zod.string()]))
              .optional()
              .describe(
                "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
              ),
          })
          .describe(
            "User's rating\/evaluation of an AI response.\n\nThis captures various forms of end-user feedback about a model's response, including\nbinary thumbs up\/down ratings, numeric scores, free-text opinions, suggested rewrites,\nand structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfields are optional to accommodate different feedback collection patterns."
          )
          .optional()
          .describe("User's rating\/evaluation"),
        events: zod
          .array(
            zod.union([
              zod
                .object({
                  id: zod
                    .string()
                    .optional()
                    .describe(
                      'Unique identifier for the event. Populated when retrieved from database.'
                    ),
                  created_at: zod
                    .string()
                    .datetime({})
                    .optional()
                    .describe('UTC timestamp when the record was created.'),
                  created_by: zod
                    .record(zod.string(), zod.string())
                    .optional()
                    .describe(
                      'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                    ),
                  event_type: zod
                    .literal('user_feedback')
                    .default(listEntriesResponseDataItemEventsItemOneEventTypeDefault),
                  thumb: zod
                    .enum(['up', 'down'])
                    .describe('Possible thumb feedback choices.')
                    .optional()
                    .describe(
                      'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                    ),
                  rating: zod
                    .number()
                    .min(listEntriesResponseDataItemEventsItemOneRatingMin)
                    .optional()
                    .describe(
                      'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                    ),
                  opinion: zod
                    .string()
                    .min(1)
                    .max(listEntriesResponseDataItemEventsItemOneOpinionMax)
                    .optional()
                    .describe(
                      'Free-text comment from the end user describing their opinion of the response.'
                    ),
                  rewrite: zod
                    .string()
                    .min(1)
                    .max(listEntriesResponseDataItemEventsItemOneRewriteMax)
                    .optional()
                    .describe(
                      "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                    ),
                  chosen_index: zod
                    .number()
                    .min(listEntriesResponseDataItemEventsItemOneChosenIndexMin)
                    .optional()
                    .describe(
                      'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                    ),
                  categories: zod
                    .record(zod.string(), zod.union([zod.number(), zod.string()]))
                    .optional()
                    .describe(
                      "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                    ),
                })
                .describe(
                  "Structured feedback supplied by an end-user.\n\nThis event captures various forms of end-user feedback about a model's response,\nincluding binary thumbs up\/down ratings, numeric scores, free-text opinions,\nsuggested rewrites, and structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfeedback fields are optional to accommodate different feedback collection patterns."
                ),
              zod
                .object({
                  id: zod
                    .string()
                    .optional()
                    .describe(
                      'Unique identifier for the event. Populated when retrieved from database.'
                    ),
                  created_at: zod
                    .string()
                    .datetime({})
                    .optional()
                    .describe('UTC timestamp when the record was created.'),
                  created_by: zod
                    .record(zod.string(), zod.string())
                    .optional()
                    .describe(
                      'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                    ),
                  event_type: zod
                    .literal('user_action')
                    .default(listEntriesResponseDataItemEventsItemTwoEventTypeDefault),
                  action: zod
                    .string()
                    .max(listEntriesResponseDataItemEventsItemTwoActionMax)
                    .describe(
                      "Descriptive name for the action taken by the user (e.g., 'share_clicked', 'code_copied', 'link_followed'). Use snake-case or kebab-case. This is a label, not a unique identifier—multiple events can have the same action name."
                    ),
                  metadata: zod
                    .record(
                      zod.string(),
                      zod.union([
                        zod.string().max(listEntriesResponseDataItemEventsItemTwoMetadataOneMax),
                        zod.array(
                          zod
                            .string()
                            .max(listEntriesResponseDataItemEventsItemTwoMetadataTwoItemMax)
                        ),
                        zod.boolean(),
                        zod.number(),
                        zod.number(),
                      ])
                    )
                    .optional()
                    .describe(
                      "Optional key-value pairs with additional context about the action (max 16 entries). Use this for details like user IDs, item IDs, timestamps, A\/B test variants, or any other information useful for downstream training or evaluation pipelines. Example: {'user_id': '12345', 'experiment': 'variant_b', 'item_purchased': 'product_456'}."
                    ),
                })
                .describe(
                  'Free-form user action captured by the client application.\n\nUse this to track arbitrary user interactions with AI responses, such as copying code,\nclicking share buttons, making purchases, or any other measurable action.\n\nThe action identifier must be ≤256 characters and should use snake-case or kebab-case\nfor consistency (e.g., ``share_clicked``, ``code_copied``, ``purchase_made``).'
                ),
              zod
                .object({
                  id: zod
                    .string()
                    .optional()
                    .describe(
                      'Unique identifier for the event. Populated when retrieved from database.'
                    ),
                  created_at: zod
                    .string()
                    .datetime({})
                    .optional()
                    .describe('UTC timestamp when the record was created.'),
                  created_by: zod
                    .record(zod.string(), zod.string())
                    .optional()
                    .describe(
                      'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                    ),
                  event_type: zod
                    .literal('reviewer_annotation')
                    .default(listEntriesResponseDataItemEventsItemThreeEventTypeDefault),
                  thumb: zod
                    .enum(['up', 'down'])
                    .describe('Possible thumb feedback choices.')
                    .optional()
                    .describe(
                      'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                    ),
                  rating: zod
                    .number()
                    .min(listEntriesResponseDataItemEventsItemThreeRatingMin)
                    .optional()
                    .describe(
                      'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                    ),
                  opinion: zod
                    .string()
                    .min(1)
                    .max(listEntriesResponseDataItemEventsItemThreeOpinionMax)
                    .optional()
                    .describe(
                      'Free-text comment from the end user describing their opinion of the response.'
                    ),
                  rewrite: zod
                    .string()
                    .min(1)
                    .max(listEntriesResponseDataItemEventsItemThreeRewriteMax)
                    .optional()
                    .describe(
                      "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                    ),
                  chosen_index: zod
                    .number()
                    .min(listEntriesResponseDataItemEventsItemThreeChosenIndexMin)
                    .optional()
                    .describe(
                      'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                    ),
                  categories: zod
                    .record(zod.string(), zod.union([zod.number(), zod.string()]))
                    .optional()
                    .describe(
                      "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                    ),
                  response_override: zod
                    .record(zod.string(), zod.unknown())
                    .optional()
                    .describe(
                      "Complete JSON object that replaces the original model response when exporting data. Unlike the `rewrite` field (which is just text), this can include tool calls, function outputs, and all other response metadata. When an entry with response_override is exported, you can choose to use this corrected response instead of the original. Example: {'choices': [{'message': {'role': 'assistant', 'content': 'Corrected text', 'tool_calls': [...]}}]}"
                    ),
                })
                .describe(
                  'Structured annotation supplied by a reviewer or expert evaluator.\n\nA reviewer annotation is similar to user feedback but includes an additional capability\nto provide a complete replacement response. This is useful when human experts need to\ncorrect not just the text but also structured elements like tool calls, function outputs,\nor other response metadata.\n\nInherits all feedback fields from UserFeedbackEvent (thumb, rating, opinion, rewrite,\nchosen_index, categories) and adds response_override for full response replacement.'
                ),
              zod
                .object({
                  id: zod
                    .string()
                    .optional()
                    .describe(
                      'Unique identifier for the event. Populated when retrieved from database.'
                    ),
                  created_at: zod
                    .string()
                    .datetime({})
                    .optional()
                    .describe('UTC timestamp when the record was created.'),
                  created_by: zod
                    .record(zod.string(), zod.string())
                    .optional()
                    .describe(
                      'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                    ),
                  event_type: zod
                    .literal('evaluator_result')
                    .default(listEntriesResponseDataItemEventsItemFourEventTypeDefault),
                  name: zod
                    .string()
                    .max(listEntriesResponseDataItemEventsItemFourNameMax)
                    .describe(
                      "Identifier of the evaluator that produced this result (e.g., 'harbor.verifier', 'evaluator.llm_judge', 'auditor.pii_probe')."
                    ),
                  score: zod
                    .union([zod.number(), zod.number(), zod.string()])
                    .optional()
                    .describe(
                      "The result value: a number (e.g., reward, rating, probability) or a string (e.g., 'pass', 'fail', a category label). Semantics are defined by the evaluator."
                    ),
                  metadata: zod
                    .record(zod.string(), zod.unknown())
                    .optional()
                    .describe(
                      'Free-form additional context about this result (e.g., supporting metrics, trial_name, rubric_version, evaluator config snapshot).'
                    ),
                })
                .describe(
                  'Result produced by an automated evaluator.\n\nUse this for any non-human scorer that emits a judgement about an entry:\neval-framework verifier rewards, LLM-judge ratings, auditor probe results, etc.\nDistinct from UserFeedbackEvent (human end-user feedback) and ReviewerAnnotationEvent\n(human expert annotation).'
                ),
            ])
          )
          .optional()
          .describe('Events associated with this entry'),
        custom_fields: zod
          .record(zod.string(), zod.unknown())
          .optional()
          .describe('Free-form metadata bag for client-defined fields.'),
        created_at: zod.string().datetime({}).optional().describe('Creation timestamp'),
        updated_at: zod.string().datetime({}).optional().describe('Last update timestamp'),
      })
      .describe('Schema for Entry responses.')
  ),
  pagination: zod
    .object({
      page: zod.number().describe('The current page number.'),
      page_size: zod.number().describe('The page size used for the query.'),
      current_page_size: zod.number().describe('The size for the current page.'),
      total_pages: zod.number().describe('The total number of pages.'),
      total_results: zod.number().describe('The total number of results.'),
    })
    .optional()
    .describe('Pagination information.'),
  sort: zod.string().optional().describe('The field on which the results are sorted.'),
  filter: zod.record(zod.string(), zod.unknown()).optional().describe('Filtering information.'),
});

/**
 * Create a new entry.

Apps and tasks referenced in the entry context will be auto-created if they don't exist.
 * @summary Create Entry
 */
export const CreateEntryParams = zod.object({
  workspace: zod.string(),
});

export const createEntryBodyUsageOneLatencyMsMin = 0;

export const createEntryBodyUsageOneCostUsdMin = 0;

export const createEntryBodyUsageOneCostInputUsdMin = 0;

export const createEntryBodyUsageOneCostOutputUsdMin = 0;

export const createEntryBodyUsageOneInputTokensMin = 0;

export const createEntryBodyUsageOneOutputTokensMin = 0;

export const createEntryBodyUsageOneCachedTokensMin = 0;

export const createEntryBodyUserRatingOneRatingMin = 0;

export const createEntryBodyUserRatingOneOpinionMax = 2000;

export const createEntryBodyUserRatingOneRewriteMax = 10000;

export const createEntryBodyUserRatingOneChosenIndexMin = 0;

export const createEntryBodyEventsItemOneEventTypeDefault = `user_feedback`;
export const createEntryBodyEventsItemOneRatingMin = 0;

export const createEntryBodyEventsItemOneOpinionMax = 2000;

export const createEntryBodyEventsItemOneRewriteMax = 10000;

export const createEntryBodyEventsItemOneChosenIndexMin = 0;

export const createEntryBodyEventsItemTwoEventTypeDefault = `user_action`;
export const createEntryBodyEventsItemTwoActionMax = 256;

export const createEntryBodyEventsItemTwoMetadataOneMax = 256;

export const createEntryBodyEventsItemTwoMetadataTwoItemMax = 256;

export const createEntryBodyEventsItemThreeEventTypeDefault = `reviewer_annotation`;
export const createEntryBodyEventsItemThreeRatingMin = 0;

export const createEntryBodyEventsItemThreeOpinionMax = 2000;

export const createEntryBodyEventsItemThreeRewriteMax = 10000;

export const createEntryBodyEventsItemThreeChosenIndexMin = 0;

export const createEntryBodyEventsItemFourEventTypeDefault = `evaluator_result`;
export const createEntryBodyEventsItemFourNameMax = 256;

export const CreateEntryBody = zod
  .object({
    external_id: zod
      .string()
      .optional()
      .describe('Optional client-provided identifier (e.g., completion_id from an LLM provider)'),
    project: zod.string().optional().describe('The name of the project associated with this entry'),
    data: zod
      .object({
        request: zod
          .object({
            messages: zod
              .array(
                zod
                  .object({
                    role: zod
                      .enum(['user', 'system', 'assistant', 'developer', 'tool', 'function'])
                      .describe('Valid role values for entry request messages.')
                      .describe(
                        'The role of the message sender. Must be one of: user, system, assistant, developer, tool, or function.'
                      ),
                  })
                  .describe(
                    'A flexible message model that requires a valid role field but allows any other fields.\n\nThis flexibility enables the Intake service to store messages from various LLM providers\nand future model types without requiring schema updates. Additional fields like `content`,\n`name`, `tool_calls`, `tool_call_id`, etc. are all accepted.\n\nExamples of additional fields:\n- `content`: The message text or content\n- `name`: Name of the user or function\n- `tool_calls`: Tool\/function calls in the message\n- `tool_call_id`: ID of the tool call being responded to'
                  )
              )
              .describe(
                'A list of messages comprising the conversation. Each message must have a valid role. Additional fields like `content`, `tool_calls`, etc. are provider-specific.'
              ),
            model: zod
              .string()
              .describe(
                "The model identifier used for this request (e.g., 'gpt-4', 'llama-3-70b', 'claude-3-opus')."
              ),
          })
          .describe(
            'Flexible entry request that accepts any object shape.\n\nThis flexibility enables the Intake service to store requests from various LLM providers\n(OpenAI, Anthropic, NIM, etc.) and future model types (embeddings, multimodal, etc.)\nwithout requiring schema updates.\n\nRequired fields: `messages` and `model`\nCommon optional fields: `temperature`, `max_tokens`, `top_p`, `tools`, `tool_choice`,\n`stream`, `response_format`, etc.'
          )
          .describe('Raw request payload recorded from the client.'),
        response: zod
          .union([zod.unknown(), zod.unknown()])
          .and(
            zod.object({
              choices: zod
                .array(zod.record(zod.string(), zod.unknown()))
                .optional()
                .describe(
                  'A list of response choices generated by the model. Each choice typically contains a message object with role, content, and optional tool_calls. The structure varies by provider but commonly includes fields like `index`, `message`, and `finish_reason`.'
                ),
              error: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Error envelope returned by the provider when the call failed (e.g., OpenAI's `{message, type, code, param}` shape). Mutually exclusive with `choices`: a response carries one or the other."
                ),
            })
          )
          .describe(
            'Flexible entry response that accepts any object shape.\n\nThis flexibility enables the Intake service to store responses from various LLM providers\nand future model types without requiring schema updates.\n\nRequired: either `choices` (successful response) or `error` (failed call).\nCommon optional fields: `id`, `created`, `model`, `usage`, `system_fingerprint`, etc.'
          )
          .describe('Raw response payload generated by the model.'),
      })
      .describe('Entry data containing the request and response for an LLM interaction.')
      .describe('Entry data containing request and response'),
    usage: zod
      .object({
        model: zod
          .string()
          .optional()
          .describe(
            'The actual model that served the request (after any routing). May differ from the model in the request body.'
          ),
        started_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call started.'),
        ended_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call ended.'),
        latency_ms: zod
          .number()
          .min(createEntryBodyUsageOneLatencyMsMin)
          .optional()
          .describe('Wall-clock latency of the upstream LLM call, in milliseconds.'),
        cost_usd: zod
          .number()
          .min(createEntryBodyUsageOneCostUsdMin)
          .optional()
          .describe('Total estimated cost of this call, in USD.'),
        cost_input_usd: zod
          .number()
          .min(createEntryBodyUsageOneCostInputUsdMin)
          .optional()
          .describe('Estimated cost attributed to input tokens, in USD.'),
        cost_output_usd: zod
          .number()
          .min(createEntryBodyUsageOneCostOutputUsdMin)
          .optional()
          .describe('Estimated cost attributed to output tokens, in USD.'),
        input_tokens: zod
          .number()
          .min(createEntryBodyUsageOneInputTokensMin)
          .optional()
          .describe('Number of input tokens consumed.'),
        output_tokens: zod
          .number()
          .min(createEntryBodyUsageOneOutputTokensMin)
          .optional()
          .describe('Number of output tokens produced.'),
        cached_tokens: zod
          .number()
          .min(createEntryBodyUsageOneCachedTokensMin)
          .optional()
          .describe('Number of input tokens served from a prompt cache (subset of input_tokens).'),
      })
      .describe(
        'Structured usage metrics captured at log time.\n\nEvery field is optional so producers can populate whatever they have without\nschema breakage. Stored as the entry-level ``usage`` field so filters can\nreach it via ``data.usage.<field>`` entity-store paths.'
      )
      .optional()
      .describe('Structured usage metrics (model served, latency, cost, token counts).'),
    context: zod
      .object({
        app: zod
          .string()
          .describe(
            "Reference to the app that produced this entry, in the form `workspace\/name`. If the app doesn't exist, it will be automatically created when the entry is ingested."
          ),
        task: zod
          .string()
          .describe(
            "Name of the task within the app (e.g., 'chat', 'completion', 'tool-call'). If the task doesn't exist, it will be automatically created when the entry is ingested."
          ),
        thread_id: zod
          .string()
          .optional()
          .describe(
            'Logical thread identifier that groups related entries in a multi-turn conversation. If provided, entries with the same thread_id are treated as part of the same conversation. If omitted, the entry is treated as a single-turn interaction (e.g., a standalone tool call or completion).'
          ),
        user_id: zod
          .string()
          .optional()
          .describe(
            "Identifier of the application's end-user who triggered this LLM interaction. This represents the person using your application (e.g., 'customer_123', 'employee@company.com'), NOT the service account that created the entry record (see ownership.created_by for that). Use this to track which of your users a conversation belongs to, filter entries by user, and enable per-user analytics. Format is application-defined."
          ),
        trace_id: zod
          .string()
          .optional()
          .describe(
            'Distributed trace identifier (e.g., W3C traceparent). Intake stores it verbatim and does not use it at ingestion time; helps with later cross-system joins.'
          ),
        session_id: zod
          .string()
          .optional()
          .describe(
            'Long-lived session identifier (e.g., user account or browser session). Stored for post-processing analytics; not used by the Intake service at runtime.'
          ),
        created_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the entry was created.'),
      })
      .describe(
        'Contextual metadata attached to every entry record.\n\nKeeping these grouped in a dedicated object avoids polluting the top-level\nentity schema and makes it trivial to extend without breaking compatibility.'
      )
      .describe('Metadata describing producer, task, trace'),
    user_rating: zod
      .object({
        thumb: zod
          .enum(['up', 'down'])
          .describe('Possible thumb feedback choices.')
          .optional()
          .describe(
            'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
          ),
        rating: zod
          .number()
          .min(createEntryBodyUserRatingOneRatingMin)
          .optional()
          .describe(
            'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
          ),
        opinion: zod
          .string()
          .min(1)
          .max(createEntryBodyUserRatingOneOpinionMax)
          .optional()
          .describe(
            'Free-text comment from the end user describing their opinion of the response.'
          ),
        rewrite: zod
          .string()
          .min(1)
          .max(createEntryBodyUserRatingOneRewriteMax)
          .optional()
          .describe(
            "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
          ),
        chosen_index: zod
          .number()
          .min(createEntryBodyUserRatingOneChosenIndexMin)
          .optional()
          .describe(
            'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
          ),
        categories: zod
          .record(zod.string(), zod.union([zod.number(), zod.string()]))
          .optional()
          .describe(
            "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
          ),
      })
      .describe(
        "User's rating\/evaluation of an AI response.\n\nThis captures various forms of end-user feedback about a model's response, including\nbinary thumbs up\/down ratings, numeric scores, free-text opinions, suggested rewrites,\nand structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfields are optional to accommodate different feedback collection patterns."
      )
      .optional()
      .describe("User's rating\/evaluation of the AI response"),
    events: zod
      .array(
        zod.union([
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_feedback')
                .default(createEntryBodyEventsItemOneEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(createEntryBodyEventsItemOneRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(createEntryBodyEventsItemOneOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(createEntryBodyEventsItemOneRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(createEntryBodyEventsItemOneChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
            })
            .describe(
              "Structured feedback supplied by an end-user.\n\nThis event captures various forms of end-user feedback about a model's response,\nincluding binary thumbs up\/down ratings, numeric scores, free-text opinions,\nsuggested rewrites, and structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfeedback fields are optional to accommodate different feedback collection patterns."
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_action')
                .default(createEntryBodyEventsItemTwoEventTypeDefault),
              action: zod
                .string()
                .max(createEntryBodyEventsItemTwoActionMax)
                .describe(
                  "Descriptive name for the action taken by the user (e.g., 'share_clicked', 'code_copied', 'link_followed'). Use snake-case or kebab-case. This is a label, not a unique identifier—multiple events can have the same action name."
                ),
              metadata: zod
                .record(
                  zod.string(),
                  zod.union([
                    zod.string().max(createEntryBodyEventsItemTwoMetadataOneMax),
                    zod.array(zod.string().max(createEntryBodyEventsItemTwoMetadataTwoItemMax)),
                    zod.boolean(),
                    zod.number(),
                    zod.number(),
                  ])
                )
                .optional()
                .describe(
                  "Optional key-value pairs with additional context about the action (max 16 entries). Use this for details like user IDs, item IDs, timestamps, A\/B test variants, or any other information useful for downstream training or evaluation pipelines. Example: {'user_id': '12345', 'experiment': 'variant_b', 'item_purchased': 'product_456'}."
                ),
            })
            .describe(
              'Free-form user action captured by the client application.\n\nUse this to track arbitrary user interactions with AI responses, such as copying code,\nclicking share buttons, making purchases, or any other measurable action.\n\nThe action identifier must be ≤256 characters and should use snake-case or kebab-case\nfor consistency (e.g., ``share_clicked``, ``code_copied``, ``purchase_made``).'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('reviewer_annotation')
                .default(createEntryBodyEventsItemThreeEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(createEntryBodyEventsItemThreeRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(createEntryBodyEventsItemThreeOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(createEntryBodyEventsItemThreeRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(createEntryBodyEventsItemThreeChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
              response_override: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Complete JSON object that replaces the original model response when exporting data. Unlike the `rewrite` field (which is just text), this can include tool calls, function outputs, and all other response metadata. When an entry with response_override is exported, you can choose to use this corrected response instead of the original. Example: {'choices': [{'message': {'role': 'assistant', 'content': 'Corrected text', 'tool_calls': [...]}}]}"
                ),
            })
            .describe(
              'Structured annotation supplied by a reviewer or expert evaluator.\n\nA reviewer annotation is similar to user feedback but includes an additional capability\nto provide a complete replacement response. This is useful when human experts need to\ncorrect not just the text but also structured elements like tool calls, function outputs,\nor other response metadata.\n\nInherits all feedback fields from UserFeedbackEvent (thumb, rating, opinion, rewrite,\nchosen_index, categories) and adds response_override for full response replacement.'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('evaluator_result')
                .default(createEntryBodyEventsItemFourEventTypeDefault),
              name: zod
                .string()
                .max(createEntryBodyEventsItemFourNameMax)
                .describe(
                  "Identifier of the evaluator that produced this result (e.g., 'harbor.verifier', 'evaluator.llm_judge', 'auditor.pii_probe')."
                ),
              score: zod
                .union([zod.number(), zod.number(), zod.string()])
                .optional()
                .describe(
                  "The result value: a number (e.g., reward, rating, probability) or a string (e.g., 'pass', 'fail', a category label). Semantics are defined by the evaluator."
                ),
              metadata: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  'Free-form additional context about this result (e.g., supporting metrics, trial_name, rubric_version, evaluator config snapshot).'
                ),
            })
            .describe(
              'Result produced by an automated evaluator.\n\nUse this for any non-human scorer that emits a judgement about an entry:\neval-framework verifier rewards, LLM-judge ratings, auditor probe results, etc.\nDistinct from UserFeedbackEvent (human end-user feedback) and ReviewerAnnotationEvent\n(human expert annotation).'
            ),
        ])
      )
      .optional()
      .describe('Events associated with this entry'),
    custom_fields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe(
        'Free-form metadata bag for client-defined fields (e.g., external experiment metadata).'
      ),
  })
  .describe('Schema for creating a new Entry.');

/**
 * Delete a specific event from an entry.

Entry can be referenced by ID or external_id using `external:{external_id}` prefix.
 * @summary Delete Event
 */
export const DeleteEventParams = zod.object({
  workspace: zod.string(),
  entry: zod.string(),
  name: zod.string(),
});

export const deleteEventResponseUsageOneLatencyMsMin = 0;

export const deleteEventResponseUsageOneCostUsdMin = 0;

export const deleteEventResponseUsageOneCostInputUsdMin = 0;

export const deleteEventResponseUsageOneCostOutputUsdMin = 0;

export const deleteEventResponseUsageOneInputTokensMin = 0;

export const deleteEventResponseUsageOneOutputTokensMin = 0;

export const deleteEventResponseUsageOneCachedTokensMin = 0;

export const deleteEventResponseUserRatingOneRatingMin = 0;

export const deleteEventResponseUserRatingOneOpinionMax = 2000;

export const deleteEventResponseUserRatingOneRewriteMax = 10000;

export const deleteEventResponseUserRatingOneChosenIndexMin = 0;

export const deleteEventResponseEventsItemOneEventTypeDefault = `user_feedback`;
export const deleteEventResponseEventsItemOneRatingMin = 0;

export const deleteEventResponseEventsItemOneOpinionMax = 2000;

export const deleteEventResponseEventsItemOneRewriteMax = 10000;

export const deleteEventResponseEventsItemOneChosenIndexMin = 0;

export const deleteEventResponseEventsItemTwoEventTypeDefault = `user_action`;
export const deleteEventResponseEventsItemTwoActionMax = 256;

export const deleteEventResponseEventsItemTwoMetadataOneMax = 256;

export const deleteEventResponseEventsItemTwoMetadataTwoItemMax = 256;

export const deleteEventResponseEventsItemThreeEventTypeDefault = `reviewer_annotation`;
export const deleteEventResponseEventsItemThreeRatingMin = 0;

export const deleteEventResponseEventsItemThreeOpinionMax = 2000;

export const deleteEventResponseEventsItemThreeRewriteMax = 10000;

export const deleteEventResponseEventsItemThreeChosenIndexMin = 0;

export const deleteEventResponseEventsItemFourEventTypeDefault = `evaluator_result`;
export const deleteEventResponseEventsItemFourNameMax = 256;

export const DeleteEventResponse = zod
  .object({
    id: zod.string().describe('Unique identifier'),
    name: zod.string().describe('Entry name (auto-generated)'),
    workspace: zod.string().describe('Workspace identifier'),
    external_id: zod.string().optional().describe('Client-provided identifier'),
    project: zod.string().optional().describe('The name of the project associated with this entry'),
    data: zod
      .object({
        request: zod
          .object({
            messages: zod
              .array(
                zod
                  .object({
                    role: zod
                      .enum(['user', 'system', 'assistant', 'developer', 'tool', 'function'])
                      .describe('Valid role values for entry request messages.')
                      .describe(
                        'The role of the message sender. Must be one of: user, system, assistant, developer, tool, or function.'
                      ),
                  })
                  .describe(
                    'A flexible message model that requires a valid role field but allows any other fields.\n\nThis flexibility enables the Intake service to store messages from various LLM providers\nand future model types without requiring schema updates. Additional fields like `content`,\n`name`, `tool_calls`, `tool_call_id`, etc. are all accepted.\n\nExamples of additional fields:\n- `content`: The message text or content\n- `name`: Name of the user or function\n- `tool_calls`: Tool\/function calls in the message\n- `tool_call_id`: ID of the tool call being responded to'
                  )
              )
              .describe(
                'A list of messages comprising the conversation. Each message must have a valid role. Additional fields like `content`, `tool_calls`, etc. are provider-specific.'
              ),
            model: zod
              .string()
              .describe(
                "The model identifier used for this request (e.g., 'gpt-4', 'llama-3-70b', 'claude-3-opus')."
              ),
          })
          .describe(
            'Flexible entry request that accepts any object shape.\n\nThis flexibility enables the Intake service to store requests from various LLM providers\n(OpenAI, Anthropic, NIM, etc.) and future model types (embeddings, multimodal, etc.)\nwithout requiring schema updates.\n\nRequired fields: `messages` and `model`\nCommon optional fields: `temperature`, `max_tokens`, `top_p`, `tools`, `tool_choice`,\n`stream`, `response_format`, etc.'
          )
          .describe('Raw request payload recorded from the client.'),
        response: zod
          .union([zod.unknown(), zod.unknown()])
          .and(
            zod.object({
              choices: zod
                .array(zod.record(zod.string(), zod.unknown()))
                .optional()
                .describe(
                  'A list of response choices generated by the model. Each choice typically contains a message object with role, content, and optional tool_calls. The structure varies by provider but commonly includes fields like `index`, `message`, and `finish_reason`.'
                ),
              error: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Error envelope returned by the provider when the call failed (e.g., OpenAI's `{message, type, code, param}` shape). Mutually exclusive with `choices`: a response carries one or the other."
                ),
            })
          )
          .describe(
            'Flexible entry response that accepts any object shape.\n\nThis flexibility enables the Intake service to store responses from various LLM providers\nand future model types without requiring schema updates.\n\nRequired: either `choices` (successful response) or `error` (failed call).\nCommon optional fields: `id`, `created`, `model`, `usage`, `system_fingerprint`, etc.'
          )
          .describe('Raw response payload generated by the model.'),
      })
      .describe('Entry data containing the request and response for an LLM interaction.')
      .describe('Entry data containing request and response'),
    usage: zod
      .object({
        model: zod
          .string()
          .optional()
          .describe(
            'The actual model that served the request (after any routing). May differ from the model in the request body.'
          ),
        started_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call started.'),
        ended_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call ended.'),
        latency_ms: zod
          .number()
          .min(deleteEventResponseUsageOneLatencyMsMin)
          .optional()
          .describe('Wall-clock latency of the upstream LLM call, in milliseconds.'),
        cost_usd: zod
          .number()
          .min(deleteEventResponseUsageOneCostUsdMin)
          .optional()
          .describe('Total estimated cost of this call, in USD.'),
        cost_input_usd: zod
          .number()
          .min(deleteEventResponseUsageOneCostInputUsdMin)
          .optional()
          .describe('Estimated cost attributed to input tokens, in USD.'),
        cost_output_usd: zod
          .number()
          .min(deleteEventResponseUsageOneCostOutputUsdMin)
          .optional()
          .describe('Estimated cost attributed to output tokens, in USD.'),
        input_tokens: zod
          .number()
          .min(deleteEventResponseUsageOneInputTokensMin)
          .optional()
          .describe('Number of input tokens consumed.'),
        output_tokens: zod
          .number()
          .min(deleteEventResponseUsageOneOutputTokensMin)
          .optional()
          .describe('Number of output tokens produced.'),
        cached_tokens: zod
          .number()
          .min(deleteEventResponseUsageOneCachedTokensMin)
          .optional()
          .describe('Number of input tokens served from a prompt cache (subset of input_tokens).'),
      })
      .describe(
        'Structured usage metrics captured at log time.\n\nEvery field is optional so producers can populate whatever they have without\nschema breakage. Stored as the entry-level ``usage`` field so filters can\nreach it via ``data.usage.<field>`` entity-store paths.'
      )
      .optional()
      .describe('Structured usage metrics (model served, latency, cost, token counts).'),
    context: zod
      .object({
        app: zod
          .string()
          .describe(
            "Reference to the app that produced this entry, in the form `workspace\/name`. If the app doesn't exist, it will be automatically created when the entry is ingested."
          ),
        task: zod
          .string()
          .describe(
            "Name of the task within the app (e.g., 'chat', 'completion', 'tool-call'). If the task doesn't exist, it will be automatically created when the entry is ingested."
          ),
        thread_id: zod
          .string()
          .optional()
          .describe(
            'Logical thread identifier that groups related entries in a multi-turn conversation. If provided, entries with the same thread_id are treated as part of the same conversation. If omitted, the entry is treated as a single-turn interaction (e.g., a standalone tool call or completion).'
          ),
        user_id: zod
          .string()
          .optional()
          .describe(
            "Identifier of the application's end-user who triggered this LLM interaction. This represents the person using your application (e.g., 'customer_123', 'employee@company.com'), NOT the service account that created the entry record (see ownership.created_by for that). Use this to track which of your users a conversation belongs to, filter entries by user, and enable per-user analytics. Format is application-defined."
          ),
        trace_id: zod
          .string()
          .optional()
          .describe(
            'Distributed trace identifier (e.g., W3C traceparent). Intake stores it verbatim and does not use it at ingestion time; helps with later cross-system joins.'
          ),
        session_id: zod
          .string()
          .optional()
          .describe(
            'Long-lived session identifier (e.g., user account or browser session). Stored for post-processing analytics; not used by the Intake service at runtime.'
          ),
        created_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the entry was created.'),
      })
      .describe(
        'Contextual metadata attached to every entry record.\n\nKeeping these grouped in a dedicated object avoids polluting the top-level\nentity schema and makes it trivial to extend without breaking compatibility.'
      )
      .describe('Metadata describing producer, task, trace'),
    user_rating: zod
      .object({
        thumb: zod
          .enum(['up', 'down'])
          .describe('Possible thumb feedback choices.')
          .optional()
          .describe(
            'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
          ),
        rating: zod
          .number()
          .min(deleteEventResponseUserRatingOneRatingMin)
          .optional()
          .describe(
            'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
          ),
        opinion: zod
          .string()
          .min(1)
          .max(deleteEventResponseUserRatingOneOpinionMax)
          .optional()
          .describe(
            'Free-text comment from the end user describing their opinion of the response.'
          ),
        rewrite: zod
          .string()
          .min(1)
          .max(deleteEventResponseUserRatingOneRewriteMax)
          .optional()
          .describe(
            "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
          ),
        chosen_index: zod
          .number()
          .min(deleteEventResponseUserRatingOneChosenIndexMin)
          .optional()
          .describe(
            'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
          ),
        categories: zod
          .record(zod.string(), zod.union([zod.number(), zod.string()]))
          .optional()
          .describe(
            "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
          ),
      })
      .describe(
        "User's rating\/evaluation of an AI response.\n\nThis captures various forms of end-user feedback about a model's response, including\nbinary thumbs up\/down ratings, numeric scores, free-text opinions, suggested rewrites,\nand structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfields are optional to accommodate different feedback collection patterns."
      )
      .optional()
      .describe("User's rating\/evaluation"),
    events: zod
      .array(
        zod.union([
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_feedback')
                .default(deleteEventResponseEventsItemOneEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(deleteEventResponseEventsItemOneRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(deleteEventResponseEventsItemOneOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(deleteEventResponseEventsItemOneRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(deleteEventResponseEventsItemOneChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
            })
            .describe(
              "Structured feedback supplied by an end-user.\n\nThis event captures various forms of end-user feedback about a model's response,\nincluding binary thumbs up\/down ratings, numeric scores, free-text opinions,\nsuggested rewrites, and structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfeedback fields are optional to accommodate different feedback collection patterns."
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_action')
                .default(deleteEventResponseEventsItemTwoEventTypeDefault),
              action: zod
                .string()
                .max(deleteEventResponseEventsItemTwoActionMax)
                .describe(
                  "Descriptive name for the action taken by the user (e.g., 'share_clicked', 'code_copied', 'link_followed'). Use snake-case or kebab-case. This is a label, not a unique identifier—multiple events can have the same action name."
                ),
              metadata: zod
                .record(
                  zod.string(),
                  zod.union([
                    zod.string().max(deleteEventResponseEventsItemTwoMetadataOneMax),
                    zod.array(zod.string().max(deleteEventResponseEventsItemTwoMetadataTwoItemMax)),
                    zod.boolean(),
                    zod.number(),
                    zod.number(),
                  ])
                )
                .optional()
                .describe(
                  "Optional key-value pairs with additional context about the action (max 16 entries). Use this for details like user IDs, item IDs, timestamps, A\/B test variants, or any other information useful for downstream training or evaluation pipelines. Example: {'user_id': '12345', 'experiment': 'variant_b', 'item_purchased': 'product_456'}."
                ),
            })
            .describe(
              'Free-form user action captured by the client application.\n\nUse this to track arbitrary user interactions with AI responses, such as copying code,\nclicking share buttons, making purchases, or any other measurable action.\n\nThe action identifier must be ≤256 characters and should use snake-case or kebab-case\nfor consistency (e.g., ``share_clicked``, ``code_copied``, ``purchase_made``).'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('reviewer_annotation')
                .default(deleteEventResponseEventsItemThreeEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(deleteEventResponseEventsItemThreeRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(deleteEventResponseEventsItemThreeOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(deleteEventResponseEventsItemThreeRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(deleteEventResponseEventsItemThreeChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
              response_override: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Complete JSON object that replaces the original model response when exporting data. Unlike the `rewrite` field (which is just text), this can include tool calls, function outputs, and all other response metadata. When an entry with response_override is exported, you can choose to use this corrected response instead of the original. Example: {'choices': [{'message': {'role': 'assistant', 'content': 'Corrected text', 'tool_calls': [...]}}]}"
                ),
            })
            .describe(
              'Structured annotation supplied by a reviewer or expert evaluator.\n\nA reviewer annotation is similar to user feedback but includes an additional capability\nto provide a complete replacement response. This is useful when human experts need to\ncorrect not just the text but also structured elements like tool calls, function outputs,\nor other response metadata.\n\nInherits all feedback fields from UserFeedbackEvent (thumb, rating, opinion, rewrite,\nchosen_index, categories) and adds response_override for full response replacement.'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('evaluator_result')
                .default(deleteEventResponseEventsItemFourEventTypeDefault),
              name: zod
                .string()
                .max(deleteEventResponseEventsItemFourNameMax)
                .describe(
                  "Identifier of the evaluator that produced this result (e.g., 'harbor.verifier', 'evaluator.llm_judge', 'auditor.pii_probe')."
                ),
              score: zod
                .union([zod.number(), zod.number(), zod.string()])
                .optional()
                .describe(
                  "The result value: a number (e.g., reward, rating, probability) or a string (e.g., 'pass', 'fail', a category label). Semantics are defined by the evaluator."
                ),
              metadata: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  'Free-form additional context about this result (e.g., supporting metrics, trial_name, rubric_version, evaluator config snapshot).'
                ),
            })
            .describe(
              'Result produced by an automated evaluator.\n\nUse this for any non-human scorer that emits a judgement about an entry:\neval-framework verifier rewards, LLM-judge ratings, auditor probe results, etc.\nDistinct from UserFeedbackEvent (human end-user feedback) and ReviewerAnnotationEvent\n(human expert annotation).'
            ),
        ])
      )
      .optional()
      .describe('Events associated with this entry'),
    custom_fields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe('Free-form metadata bag for client-defined fields.'),
    created_at: zod.string().datetime({}).optional().describe('Creation timestamp'),
    updated_at: zod.string().datetime({}).optional().describe('Last update timestamp'),
  })
  .describe('Schema for Entry responses.');

/**
 * Get a specific entry by ID or external_id.

Use `external:{external_id}` to get by external_id.
Example: `/v2/workspaces/{workspace}/entries/external:chatcmpl-abc123`
 * @summary Get Entry
 */
export const GetEntryParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

export const getEntryResponseUsageOneLatencyMsMin = 0;

export const getEntryResponseUsageOneCostUsdMin = 0;

export const getEntryResponseUsageOneCostInputUsdMin = 0;

export const getEntryResponseUsageOneCostOutputUsdMin = 0;

export const getEntryResponseUsageOneInputTokensMin = 0;

export const getEntryResponseUsageOneOutputTokensMin = 0;

export const getEntryResponseUsageOneCachedTokensMin = 0;

export const getEntryResponseUserRatingOneRatingMin = 0;

export const getEntryResponseUserRatingOneOpinionMax = 2000;

export const getEntryResponseUserRatingOneRewriteMax = 10000;

export const getEntryResponseUserRatingOneChosenIndexMin = 0;

export const getEntryResponseEventsItemOneEventTypeDefault = `user_feedback`;
export const getEntryResponseEventsItemOneRatingMin = 0;

export const getEntryResponseEventsItemOneOpinionMax = 2000;

export const getEntryResponseEventsItemOneRewriteMax = 10000;

export const getEntryResponseEventsItemOneChosenIndexMin = 0;

export const getEntryResponseEventsItemTwoEventTypeDefault = `user_action`;
export const getEntryResponseEventsItemTwoActionMax = 256;

export const getEntryResponseEventsItemTwoMetadataOneMax = 256;

export const getEntryResponseEventsItemTwoMetadataTwoItemMax = 256;

export const getEntryResponseEventsItemThreeEventTypeDefault = `reviewer_annotation`;
export const getEntryResponseEventsItemThreeRatingMin = 0;

export const getEntryResponseEventsItemThreeOpinionMax = 2000;

export const getEntryResponseEventsItemThreeRewriteMax = 10000;

export const getEntryResponseEventsItemThreeChosenIndexMin = 0;

export const getEntryResponseEventsItemFourEventTypeDefault = `evaluator_result`;
export const getEntryResponseEventsItemFourNameMax = 256;

export const GetEntryResponse = zod
  .object({
    id: zod.string().describe('Unique identifier'),
    name: zod.string().describe('Entry name (auto-generated)'),
    workspace: zod.string().describe('Workspace identifier'),
    external_id: zod.string().optional().describe('Client-provided identifier'),
    project: zod.string().optional().describe('The name of the project associated with this entry'),
    data: zod
      .object({
        request: zod
          .object({
            messages: zod
              .array(
                zod
                  .object({
                    role: zod
                      .enum(['user', 'system', 'assistant', 'developer', 'tool', 'function'])
                      .describe('Valid role values for entry request messages.')
                      .describe(
                        'The role of the message sender. Must be one of: user, system, assistant, developer, tool, or function.'
                      ),
                  })
                  .describe(
                    'A flexible message model that requires a valid role field but allows any other fields.\n\nThis flexibility enables the Intake service to store messages from various LLM providers\nand future model types without requiring schema updates. Additional fields like `content`,\n`name`, `tool_calls`, `tool_call_id`, etc. are all accepted.\n\nExamples of additional fields:\n- `content`: The message text or content\n- `name`: Name of the user or function\n- `tool_calls`: Tool\/function calls in the message\n- `tool_call_id`: ID of the tool call being responded to'
                  )
              )
              .describe(
                'A list of messages comprising the conversation. Each message must have a valid role. Additional fields like `content`, `tool_calls`, etc. are provider-specific.'
              ),
            model: zod
              .string()
              .describe(
                "The model identifier used for this request (e.g., 'gpt-4', 'llama-3-70b', 'claude-3-opus')."
              ),
          })
          .describe(
            'Flexible entry request that accepts any object shape.\n\nThis flexibility enables the Intake service to store requests from various LLM providers\n(OpenAI, Anthropic, NIM, etc.) and future model types (embeddings, multimodal, etc.)\nwithout requiring schema updates.\n\nRequired fields: `messages` and `model`\nCommon optional fields: `temperature`, `max_tokens`, `top_p`, `tools`, `tool_choice`,\n`stream`, `response_format`, etc.'
          )
          .describe('Raw request payload recorded from the client.'),
        response: zod
          .union([zod.unknown(), zod.unknown()])
          .and(
            zod.object({
              choices: zod
                .array(zod.record(zod.string(), zod.unknown()))
                .optional()
                .describe(
                  'A list of response choices generated by the model. Each choice typically contains a message object with role, content, and optional tool_calls. The structure varies by provider but commonly includes fields like `index`, `message`, and `finish_reason`.'
                ),
              error: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Error envelope returned by the provider when the call failed (e.g., OpenAI's `{message, type, code, param}` shape). Mutually exclusive with `choices`: a response carries one or the other."
                ),
            })
          )
          .describe(
            'Flexible entry response that accepts any object shape.\n\nThis flexibility enables the Intake service to store responses from various LLM providers\nand future model types without requiring schema updates.\n\nRequired: either `choices` (successful response) or `error` (failed call).\nCommon optional fields: `id`, `created`, `model`, `usage`, `system_fingerprint`, etc.'
          )
          .describe('Raw response payload generated by the model.'),
      })
      .describe('Entry data containing the request and response for an LLM interaction.')
      .describe('Entry data containing request and response'),
    usage: zod
      .object({
        model: zod
          .string()
          .optional()
          .describe(
            'The actual model that served the request (after any routing). May differ from the model in the request body.'
          ),
        started_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call started.'),
        ended_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call ended.'),
        latency_ms: zod
          .number()
          .min(getEntryResponseUsageOneLatencyMsMin)
          .optional()
          .describe('Wall-clock latency of the upstream LLM call, in milliseconds.'),
        cost_usd: zod
          .number()
          .min(getEntryResponseUsageOneCostUsdMin)
          .optional()
          .describe('Total estimated cost of this call, in USD.'),
        cost_input_usd: zod
          .number()
          .min(getEntryResponseUsageOneCostInputUsdMin)
          .optional()
          .describe('Estimated cost attributed to input tokens, in USD.'),
        cost_output_usd: zod
          .number()
          .min(getEntryResponseUsageOneCostOutputUsdMin)
          .optional()
          .describe('Estimated cost attributed to output tokens, in USD.'),
        input_tokens: zod
          .number()
          .min(getEntryResponseUsageOneInputTokensMin)
          .optional()
          .describe('Number of input tokens consumed.'),
        output_tokens: zod
          .number()
          .min(getEntryResponseUsageOneOutputTokensMin)
          .optional()
          .describe('Number of output tokens produced.'),
        cached_tokens: zod
          .number()
          .min(getEntryResponseUsageOneCachedTokensMin)
          .optional()
          .describe('Number of input tokens served from a prompt cache (subset of input_tokens).'),
      })
      .describe(
        'Structured usage metrics captured at log time.\n\nEvery field is optional so producers can populate whatever they have without\nschema breakage. Stored as the entry-level ``usage`` field so filters can\nreach it via ``data.usage.<field>`` entity-store paths.'
      )
      .optional()
      .describe('Structured usage metrics (model served, latency, cost, token counts).'),
    context: zod
      .object({
        app: zod
          .string()
          .describe(
            "Reference to the app that produced this entry, in the form `workspace\/name`. If the app doesn't exist, it will be automatically created when the entry is ingested."
          ),
        task: zod
          .string()
          .describe(
            "Name of the task within the app (e.g., 'chat', 'completion', 'tool-call'). If the task doesn't exist, it will be automatically created when the entry is ingested."
          ),
        thread_id: zod
          .string()
          .optional()
          .describe(
            'Logical thread identifier that groups related entries in a multi-turn conversation. If provided, entries with the same thread_id are treated as part of the same conversation. If omitted, the entry is treated as a single-turn interaction (e.g., a standalone tool call or completion).'
          ),
        user_id: zod
          .string()
          .optional()
          .describe(
            "Identifier of the application's end-user who triggered this LLM interaction. This represents the person using your application (e.g., 'customer_123', 'employee@company.com'), NOT the service account that created the entry record (see ownership.created_by for that). Use this to track which of your users a conversation belongs to, filter entries by user, and enable per-user analytics. Format is application-defined."
          ),
        trace_id: zod
          .string()
          .optional()
          .describe(
            'Distributed trace identifier (e.g., W3C traceparent). Intake stores it verbatim and does not use it at ingestion time; helps with later cross-system joins.'
          ),
        session_id: zod
          .string()
          .optional()
          .describe(
            'Long-lived session identifier (e.g., user account or browser session). Stored for post-processing analytics; not used by the Intake service at runtime.'
          ),
        created_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the entry was created.'),
      })
      .describe(
        'Contextual metadata attached to every entry record.\n\nKeeping these grouped in a dedicated object avoids polluting the top-level\nentity schema and makes it trivial to extend without breaking compatibility.'
      )
      .describe('Metadata describing producer, task, trace'),
    user_rating: zod
      .object({
        thumb: zod
          .enum(['up', 'down'])
          .describe('Possible thumb feedback choices.')
          .optional()
          .describe(
            'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
          ),
        rating: zod
          .number()
          .min(getEntryResponseUserRatingOneRatingMin)
          .optional()
          .describe(
            'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
          ),
        opinion: zod
          .string()
          .min(1)
          .max(getEntryResponseUserRatingOneOpinionMax)
          .optional()
          .describe(
            'Free-text comment from the end user describing their opinion of the response.'
          ),
        rewrite: zod
          .string()
          .min(1)
          .max(getEntryResponseUserRatingOneRewriteMax)
          .optional()
          .describe(
            "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
          ),
        chosen_index: zod
          .number()
          .min(getEntryResponseUserRatingOneChosenIndexMin)
          .optional()
          .describe(
            'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
          ),
        categories: zod
          .record(zod.string(), zod.union([zod.number(), zod.string()]))
          .optional()
          .describe(
            "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
          ),
      })
      .describe(
        "User's rating\/evaluation of an AI response.\n\nThis captures various forms of end-user feedback about a model's response, including\nbinary thumbs up\/down ratings, numeric scores, free-text opinions, suggested rewrites,\nand structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfields are optional to accommodate different feedback collection patterns."
      )
      .optional()
      .describe("User's rating\/evaluation"),
    events: zod
      .array(
        zod.union([
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_feedback')
                .default(getEntryResponseEventsItemOneEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(getEntryResponseEventsItemOneRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(getEntryResponseEventsItemOneOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(getEntryResponseEventsItemOneRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(getEntryResponseEventsItemOneChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
            })
            .describe(
              "Structured feedback supplied by an end-user.\n\nThis event captures various forms of end-user feedback about a model's response,\nincluding binary thumbs up\/down ratings, numeric scores, free-text opinions,\nsuggested rewrites, and structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfeedback fields are optional to accommodate different feedback collection patterns."
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_action')
                .default(getEntryResponseEventsItemTwoEventTypeDefault),
              action: zod
                .string()
                .max(getEntryResponseEventsItemTwoActionMax)
                .describe(
                  "Descriptive name for the action taken by the user (e.g., 'share_clicked', 'code_copied', 'link_followed'). Use snake-case or kebab-case. This is a label, not a unique identifier—multiple events can have the same action name."
                ),
              metadata: zod
                .record(
                  zod.string(),
                  zod.union([
                    zod.string().max(getEntryResponseEventsItemTwoMetadataOneMax),
                    zod.array(zod.string().max(getEntryResponseEventsItemTwoMetadataTwoItemMax)),
                    zod.boolean(),
                    zod.number(),
                    zod.number(),
                  ])
                )
                .optional()
                .describe(
                  "Optional key-value pairs with additional context about the action (max 16 entries). Use this for details like user IDs, item IDs, timestamps, A\/B test variants, or any other information useful for downstream training or evaluation pipelines. Example: {'user_id': '12345', 'experiment': 'variant_b', 'item_purchased': 'product_456'}."
                ),
            })
            .describe(
              'Free-form user action captured by the client application.\n\nUse this to track arbitrary user interactions with AI responses, such as copying code,\nclicking share buttons, making purchases, or any other measurable action.\n\nThe action identifier must be ≤256 characters and should use snake-case or kebab-case\nfor consistency (e.g., ``share_clicked``, ``code_copied``, ``purchase_made``).'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('reviewer_annotation')
                .default(getEntryResponseEventsItemThreeEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(getEntryResponseEventsItemThreeRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(getEntryResponseEventsItemThreeOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(getEntryResponseEventsItemThreeRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(getEntryResponseEventsItemThreeChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
              response_override: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Complete JSON object that replaces the original model response when exporting data. Unlike the `rewrite` field (which is just text), this can include tool calls, function outputs, and all other response metadata. When an entry with response_override is exported, you can choose to use this corrected response instead of the original. Example: {'choices': [{'message': {'role': 'assistant', 'content': 'Corrected text', 'tool_calls': [...]}}]}"
                ),
            })
            .describe(
              'Structured annotation supplied by a reviewer or expert evaluator.\n\nA reviewer annotation is similar to user feedback but includes an additional capability\nto provide a complete replacement response. This is useful when human experts need to\ncorrect not just the text but also structured elements like tool calls, function outputs,\nor other response metadata.\n\nInherits all feedback fields from UserFeedbackEvent (thumb, rating, opinion, rewrite,\nchosen_index, categories) and adds response_override for full response replacement.'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('evaluator_result')
                .default(getEntryResponseEventsItemFourEventTypeDefault),
              name: zod
                .string()
                .max(getEntryResponseEventsItemFourNameMax)
                .describe(
                  "Identifier of the evaluator that produced this result (e.g., 'harbor.verifier', 'evaluator.llm_judge', 'auditor.pii_probe')."
                ),
              score: zod
                .union([zod.number(), zod.number(), zod.string()])
                .optional()
                .describe(
                  "The result value: a number (e.g., reward, rating, probability) or a string (e.g., 'pass', 'fail', a category label). Semantics are defined by the evaluator."
                ),
              metadata: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  'Free-form additional context about this result (e.g., supporting metrics, trial_name, rubric_version, evaluator config snapshot).'
                ),
            })
            .describe(
              'Result produced by an automated evaluator.\n\nUse this for any non-human scorer that emits a judgement about an entry:\neval-framework verifier rewards, LLM-judge ratings, auditor probe results, etc.\nDistinct from UserFeedbackEvent (human end-user feedback) and ReviewerAnnotationEvent\n(human expert annotation).'
            ),
        ])
      )
      .optional()
      .describe('Events associated with this entry'),
    custom_fields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe('Free-form metadata bag for client-defined fields.'),
    created_at: zod.string().datetime({}).optional().describe('Creation timestamp'),
    updated_at: zod.string().datetime({}).optional().describe('Last update timestamp'),
  })
  .describe('Schema for Entry responses.');

/**
 * Update an existing entry by ID or external_id.

Use `external:{external_id}` to update by external_id.
Example: `/v2/workspaces/{workspace}/entries/external:chatcmpl-abc123`
 * @summary Update Entry
 */
export const UpdateEntryParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

export const updateEntryBodyUsageOneLatencyMsMin = 0;

export const updateEntryBodyUsageOneCostUsdMin = 0;

export const updateEntryBodyUsageOneCostInputUsdMin = 0;

export const updateEntryBodyUsageOneCostOutputUsdMin = 0;

export const updateEntryBodyUsageOneInputTokensMin = 0;

export const updateEntryBodyUsageOneOutputTokensMin = 0;

export const updateEntryBodyUsageOneCachedTokensMin = 0;

export const updateEntryBodyUserRatingOneRatingMin = 0;

export const updateEntryBodyUserRatingOneOpinionMax = 2000;

export const updateEntryBodyUserRatingOneRewriteMax = 10000;

export const updateEntryBodyUserRatingOneChosenIndexMin = 0;

export const updateEntryBodyEventsItemOneEventTypeDefault = `user_feedback`;
export const updateEntryBodyEventsItemOneRatingMin = 0;

export const updateEntryBodyEventsItemOneOpinionMax = 2000;

export const updateEntryBodyEventsItemOneRewriteMax = 10000;

export const updateEntryBodyEventsItemOneChosenIndexMin = 0;

export const updateEntryBodyEventsItemTwoEventTypeDefault = `user_action`;
export const updateEntryBodyEventsItemTwoActionMax = 256;

export const updateEntryBodyEventsItemTwoMetadataOneMax = 256;

export const updateEntryBodyEventsItemTwoMetadataTwoItemMax = 256;

export const updateEntryBodyEventsItemThreeEventTypeDefault = `reviewer_annotation`;
export const updateEntryBodyEventsItemThreeRatingMin = 0;

export const updateEntryBodyEventsItemThreeOpinionMax = 2000;

export const updateEntryBodyEventsItemThreeRewriteMax = 10000;

export const updateEntryBodyEventsItemThreeChosenIndexMin = 0;

export const updateEntryBodyEventsItemFourEventTypeDefault = `evaluator_result`;
export const updateEntryBodyEventsItemFourNameMax = 256;

export const UpdateEntryBody = zod
  .object({
    data: zod
      .object({
        request: zod
          .object({
            messages: zod
              .array(
                zod
                  .object({
                    role: zod
                      .enum(['user', 'system', 'assistant', 'developer', 'tool', 'function'])
                      .describe('Valid role values for entry request messages.')
                      .describe(
                        'The role of the message sender. Must be one of: user, system, assistant, developer, tool, or function.'
                      ),
                  })
                  .describe(
                    'A flexible message model that requires a valid role field but allows any other fields.\n\nThis flexibility enables the Intake service to store messages from various LLM providers\nand future model types without requiring schema updates. Additional fields like `content`,\n`name`, `tool_calls`, `tool_call_id`, etc. are all accepted.\n\nExamples of additional fields:\n- `content`: The message text or content\n- `name`: Name of the user or function\n- `tool_calls`: Tool\/function calls in the message\n- `tool_call_id`: ID of the tool call being responded to'
                  )
              )
              .describe(
                'A list of messages comprising the conversation. Each message must have a valid role. Additional fields like `content`, `tool_calls`, etc. are provider-specific.'
              ),
            model: zod
              .string()
              .describe(
                "The model identifier used for this request (e.g., 'gpt-4', 'llama-3-70b', 'claude-3-opus')."
              ),
          })
          .describe(
            'Flexible entry request that accepts any object shape.\n\nThis flexibility enables the Intake service to store requests from various LLM providers\n(OpenAI, Anthropic, NIM, etc.) and future model types (embeddings, multimodal, etc.)\nwithout requiring schema updates.\n\nRequired fields: `messages` and `model`\nCommon optional fields: `temperature`, `max_tokens`, `top_p`, `tools`, `tool_choice`,\n`stream`, `response_format`, etc.'
          )
          .describe('Raw request payload recorded from the client.'),
        response: zod
          .union([zod.unknown(), zod.unknown()])
          .and(
            zod.object({
              choices: zod
                .array(zod.record(zod.string(), zod.unknown()))
                .optional()
                .describe(
                  'A list of response choices generated by the model. Each choice typically contains a message object with role, content, and optional tool_calls. The structure varies by provider but commonly includes fields like `index`, `message`, and `finish_reason`.'
                ),
              error: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Error envelope returned by the provider when the call failed (e.g., OpenAI's `{message, type, code, param}` shape). Mutually exclusive with `choices`: a response carries one or the other."
                ),
            })
          )
          .describe(
            'Flexible entry response that accepts any object shape.\n\nThis flexibility enables the Intake service to store responses from various LLM providers\nand future model types without requiring schema updates.\n\nRequired: either `choices` (successful response) or `error` (failed call).\nCommon optional fields: `id`, `created`, `model`, `usage`, `system_fingerprint`, etc.'
          )
          .describe('Raw response payload generated by the model.'),
      })
      .describe('Entry data containing the request and response for an LLM interaction.')
      .optional()
      .describe('Entry data containing request and response'),
    usage: zod
      .object({
        model: zod
          .string()
          .optional()
          .describe(
            'The actual model that served the request (after any routing). May differ from the model in the request body.'
          ),
        started_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call started.'),
        ended_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call ended.'),
        latency_ms: zod
          .number()
          .min(updateEntryBodyUsageOneLatencyMsMin)
          .optional()
          .describe('Wall-clock latency of the upstream LLM call, in milliseconds.'),
        cost_usd: zod
          .number()
          .min(updateEntryBodyUsageOneCostUsdMin)
          .optional()
          .describe('Total estimated cost of this call, in USD.'),
        cost_input_usd: zod
          .number()
          .min(updateEntryBodyUsageOneCostInputUsdMin)
          .optional()
          .describe('Estimated cost attributed to input tokens, in USD.'),
        cost_output_usd: zod
          .number()
          .min(updateEntryBodyUsageOneCostOutputUsdMin)
          .optional()
          .describe('Estimated cost attributed to output tokens, in USD.'),
        input_tokens: zod
          .number()
          .min(updateEntryBodyUsageOneInputTokensMin)
          .optional()
          .describe('Number of input tokens consumed.'),
        output_tokens: zod
          .number()
          .min(updateEntryBodyUsageOneOutputTokensMin)
          .optional()
          .describe('Number of output tokens produced.'),
        cached_tokens: zod
          .number()
          .min(updateEntryBodyUsageOneCachedTokensMin)
          .optional()
          .describe('Number of input tokens served from a prompt cache (subset of input_tokens).'),
      })
      .describe(
        'Structured usage metrics captured at log time.\n\nEvery field is optional so producers can populate whatever they have without\nschema breakage. Stored as the entry-level ``usage`` field so filters can\nreach it via ``data.usage.<field>`` entity-store paths.'
      )
      .optional()
      .describe('Structured usage metrics (model served, latency, cost, token counts).'),
    context: zod
      .object({
        app: zod
          .string()
          .describe(
            "Reference to the app that produced this entry, in the form `workspace\/name`. If the app doesn't exist, it will be automatically created when the entry is ingested."
          ),
        task: zod
          .string()
          .describe(
            "Name of the task within the app (e.g., 'chat', 'completion', 'tool-call'). If the task doesn't exist, it will be automatically created when the entry is ingested."
          ),
        thread_id: zod
          .string()
          .optional()
          .describe(
            'Logical thread identifier that groups related entries in a multi-turn conversation. If provided, entries with the same thread_id are treated as part of the same conversation. If omitted, the entry is treated as a single-turn interaction (e.g., a standalone tool call or completion).'
          ),
        user_id: zod
          .string()
          .optional()
          .describe(
            "Identifier of the application's end-user who triggered this LLM interaction. This represents the person using your application (e.g., 'customer_123', 'employee@company.com'), NOT the service account that created the entry record (see ownership.created_by for that). Use this to track which of your users a conversation belongs to, filter entries by user, and enable per-user analytics. Format is application-defined."
          ),
        trace_id: zod
          .string()
          .optional()
          .describe(
            'Distributed trace identifier (e.g., W3C traceparent). Intake stores it verbatim and does not use it at ingestion time; helps with later cross-system joins.'
          ),
        session_id: zod
          .string()
          .optional()
          .describe(
            'Long-lived session identifier (e.g., user account or browser session). Stored for post-processing analytics; not used by the Intake service at runtime.'
          ),
        created_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the entry was created.'),
      })
      .describe(
        'Contextual metadata attached to every entry record.\n\nKeeping these grouped in a dedicated object avoids polluting the top-level\nentity schema and makes it trivial to extend without breaking compatibility.'
      )
      .optional()
      .describe('Metadata describing producer, task, trace'),
    user_rating: zod
      .object({
        thumb: zod
          .enum(['up', 'down'])
          .describe('Possible thumb feedback choices.')
          .optional()
          .describe(
            'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
          ),
        rating: zod
          .number()
          .min(updateEntryBodyUserRatingOneRatingMin)
          .optional()
          .describe(
            'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
          ),
        opinion: zod
          .string()
          .min(1)
          .max(updateEntryBodyUserRatingOneOpinionMax)
          .optional()
          .describe(
            'Free-text comment from the end user describing their opinion of the response.'
          ),
        rewrite: zod
          .string()
          .min(1)
          .max(updateEntryBodyUserRatingOneRewriteMax)
          .optional()
          .describe(
            "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
          ),
        chosen_index: zod
          .number()
          .min(updateEntryBodyUserRatingOneChosenIndexMin)
          .optional()
          .describe(
            'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
          ),
        categories: zod
          .record(zod.string(), zod.union([zod.number(), zod.string()]))
          .optional()
          .describe(
            "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
          ),
      })
      .describe(
        "User's rating\/evaluation of an AI response.\n\nThis captures various forms of end-user feedback about a model's response, including\nbinary thumbs up\/down ratings, numeric scores, free-text opinions, suggested rewrites,\nand structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfields are optional to accommodate different feedback collection patterns."
      )
      .optional()
      .describe("User's rating\/evaluation of the AI response"),
    events: zod
      .array(
        zod.union([
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_feedback')
                .default(updateEntryBodyEventsItemOneEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(updateEntryBodyEventsItemOneRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(updateEntryBodyEventsItemOneOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(updateEntryBodyEventsItemOneRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(updateEntryBodyEventsItemOneChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
            })
            .describe(
              "Structured feedback supplied by an end-user.\n\nThis event captures various forms of end-user feedback about a model's response,\nincluding binary thumbs up\/down ratings, numeric scores, free-text opinions,\nsuggested rewrites, and structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfeedback fields are optional to accommodate different feedback collection patterns."
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_action')
                .default(updateEntryBodyEventsItemTwoEventTypeDefault),
              action: zod
                .string()
                .max(updateEntryBodyEventsItemTwoActionMax)
                .describe(
                  "Descriptive name for the action taken by the user (e.g., 'share_clicked', 'code_copied', 'link_followed'). Use snake-case or kebab-case. This is a label, not a unique identifier—multiple events can have the same action name."
                ),
              metadata: zod
                .record(
                  zod.string(),
                  zod.union([
                    zod.string().max(updateEntryBodyEventsItemTwoMetadataOneMax),
                    zod.array(zod.string().max(updateEntryBodyEventsItemTwoMetadataTwoItemMax)),
                    zod.boolean(),
                    zod.number(),
                    zod.number(),
                  ])
                )
                .optional()
                .describe(
                  "Optional key-value pairs with additional context about the action (max 16 entries). Use this for details like user IDs, item IDs, timestamps, A\/B test variants, or any other information useful for downstream training or evaluation pipelines. Example: {'user_id': '12345', 'experiment': 'variant_b', 'item_purchased': 'product_456'}."
                ),
            })
            .describe(
              'Free-form user action captured by the client application.\n\nUse this to track arbitrary user interactions with AI responses, such as copying code,\nclicking share buttons, making purchases, or any other measurable action.\n\nThe action identifier must be ≤256 characters and should use snake-case or kebab-case\nfor consistency (e.g., ``share_clicked``, ``code_copied``, ``purchase_made``).'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('reviewer_annotation')
                .default(updateEntryBodyEventsItemThreeEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(updateEntryBodyEventsItemThreeRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(updateEntryBodyEventsItemThreeOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(updateEntryBodyEventsItemThreeRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(updateEntryBodyEventsItemThreeChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
              response_override: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Complete JSON object that replaces the original model response when exporting data. Unlike the `rewrite` field (which is just text), this can include tool calls, function outputs, and all other response metadata. When an entry with response_override is exported, you can choose to use this corrected response instead of the original. Example: {'choices': [{'message': {'role': 'assistant', 'content': 'Corrected text', 'tool_calls': [...]}}]}"
                ),
            })
            .describe(
              'Structured annotation supplied by a reviewer or expert evaluator.\n\nA reviewer annotation is similar to user feedback but includes an additional capability\nto provide a complete replacement response. This is useful when human experts need to\ncorrect not just the text but also structured elements like tool calls, function outputs,\nor other response metadata.\n\nInherits all feedback fields from UserFeedbackEvent (thumb, rating, opinion, rewrite,\nchosen_index, categories) and adds response_override for full response replacement.'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('evaluator_result')
                .default(updateEntryBodyEventsItemFourEventTypeDefault),
              name: zod
                .string()
                .max(updateEntryBodyEventsItemFourNameMax)
                .describe(
                  "Identifier of the evaluator that produced this result (e.g., 'harbor.verifier', 'evaluator.llm_judge', 'auditor.pii_probe')."
                ),
              score: zod
                .union([zod.number(), zod.number(), zod.string()])
                .optional()
                .describe(
                  "The result value: a number (e.g., reward, rating, probability) or a string (e.g., 'pass', 'fail', a category label). Semantics are defined by the evaluator."
                ),
              metadata: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  'Free-form additional context about this result (e.g., supporting metrics, trial_name, rubric_version, evaluator config snapshot).'
                ),
            })
            .describe(
              'Result produced by an automated evaluator.\n\nUse this for any non-human scorer that emits a judgement about an entry:\neval-framework verifier rewards, LLM-judge ratings, auditor probe results, etc.\nDistinct from UserFeedbackEvent (human end-user feedback) and ReviewerAnnotationEvent\n(human expert annotation).'
            ),
        ])
      )
      .optional()
      .describe('Events associated with this entry'),
    custom_fields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe(
        'Free-form metadata bag for client-defined fields (replaces existing value when provided).'
      ),
  })
  .describe('Schema for updating an existing Entry.');

export const updateEntryResponseUsageOneLatencyMsMin = 0;

export const updateEntryResponseUsageOneCostUsdMin = 0;

export const updateEntryResponseUsageOneCostInputUsdMin = 0;

export const updateEntryResponseUsageOneCostOutputUsdMin = 0;

export const updateEntryResponseUsageOneInputTokensMin = 0;

export const updateEntryResponseUsageOneOutputTokensMin = 0;

export const updateEntryResponseUsageOneCachedTokensMin = 0;

export const updateEntryResponseUserRatingOneRatingMin = 0;

export const updateEntryResponseUserRatingOneOpinionMax = 2000;

export const updateEntryResponseUserRatingOneRewriteMax = 10000;

export const updateEntryResponseUserRatingOneChosenIndexMin = 0;

export const updateEntryResponseEventsItemOneEventTypeDefault = `user_feedback`;
export const updateEntryResponseEventsItemOneRatingMin = 0;

export const updateEntryResponseEventsItemOneOpinionMax = 2000;

export const updateEntryResponseEventsItemOneRewriteMax = 10000;

export const updateEntryResponseEventsItemOneChosenIndexMin = 0;

export const updateEntryResponseEventsItemTwoEventTypeDefault = `user_action`;
export const updateEntryResponseEventsItemTwoActionMax = 256;

export const updateEntryResponseEventsItemTwoMetadataOneMax = 256;

export const updateEntryResponseEventsItemTwoMetadataTwoItemMax = 256;

export const updateEntryResponseEventsItemThreeEventTypeDefault = `reviewer_annotation`;
export const updateEntryResponseEventsItemThreeRatingMin = 0;

export const updateEntryResponseEventsItemThreeOpinionMax = 2000;

export const updateEntryResponseEventsItemThreeRewriteMax = 10000;

export const updateEntryResponseEventsItemThreeChosenIndexMin = 0;

export const updateEntryResponseEventsItemFourEventTypeDefault = `evaluator_result`;
export const updateEntryResponseEventsItemFourNameMax = 256;

export const UpdateEntryResponse = zod
  .object({
    id: zod.string().describe('Unique identifier'),
    name: zod.string().describe('Entry name (auto-generated)'),
    workspace: zod.string().describe('Workspace identifier'),
    external_id: zod.string().optional().describe('Client-provided identifier'),
    project: zod.string().optional().describe('The name of the project associated with this entry'),
    data: zod
      .object({
        request: zod
          .object({
            messages: zod
              .array(
                zod
                  .object({
                    role: zod
                      .enum(['user', 'system', 'assistant', 'developer', 'tool', 'function'])
                      .describe('Valid role values for entry request messages.')
                      .describe(
                        'The role of the message sender. Must be one of: user, system, assistant, developer, tool, or function.'
                      ),
                  })
                  .describe(
                    'A flexible message model that requires a valid role field but allows any other fields.\n\nThis flexibility enables the Intake service to store messages from various LLM providers\nand future model types without requiring schema updates. Additional fields like `content`,\n`name`, `tool_calls`, `tool_call_id`, etc. are all accepted.\n\nExamples of additional fields:\n- `content`: The message text or content\n- `name`: Name of the user or function\n- `tool_calls`: Tool\/function calls in the message\n- `tool_call_id`: ID of the tool call being responded to'
                  )
              )
              .describe(
                'A list of messages comprising the conversation. Each message must have a valid role. Additional fields like `content`, `tool_calls`, etc. are provider-specific.'
              ),
            model: zod
              .string()
              .describe(
                "The model identifier used for this request (e.g., 'gpt-4', 'llama-3-70b', 'claude-3-opus')."
              ),
          })
          .describe(
            'Flexible entry request that accepts any object shape.\n\nThis flexibility enables the Intake service to store requests from various LLM providers\n(OpenAI, Anthropic, NIM, etc.) and future model types (embeddings, multimodal, etc.)\nwithout requiring schema updates.\n\nRequired fields: `messages` and `model`\nCommon optional fields: `temperature`, `max_tokens`, `top_p`, `tools`, `tool_choice`,\n`stream`, `response_format`, etc.'
          )
          .describe('Raw request payload recorded from the client.'),
        response: zod
          .union([zod.unknown(), zod.unknown()])
          .and(
            zod.object({
              choices: zod
                .array(zod.record(zod.string(), zod.unknown()))
                .optional()
                .describe(
                  'A list of response choices generated by the model. Each choice typically contains a message object with role, content, and optional tool_calls. The structure varies by provider but commonly includes fields like `index`, `message`, and `finish_reason`.'
                ),
              error: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Error envelope returned by the provider when the call failed (e.g., OpenAI's `{message, type, code, param}` shape). Mutually exclusive with `choices`: a response carries one or the other."
                ),
            })
          )
          .describe(
            'Flexible entry response that accepts any object shape.\n\nThis flexibility enables the Intake service to store responses from various LLM providers\nand future model types without requiring schema updates.\n\nRequired: either `choices` (successful response) or `error` (failed call).\nCommon optional fields: `id`, `created`, `model`, `usage`, `system_fingerprint`, etc.'
          )
          .describe('Raw response payload generated by the model.'),
      })
      .describe('Entry data containing the request and response for an LLM interaction.')
      .describe('Entry data containing request and response'),
    usage: zod
      .object({
        model: zod
          .string()
          .optional()
          .describe(
            'The actual model that served the request (after any routing). May differ from the model in the request body.'
          ),
        started_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call started.'),
        ended_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call ended.'),
        latency_ms: zod
          .number()
          .min(updateEntryResponseUsageOneLatencyMsMin)
          .optional()
          .describe('Wall-clock latency of the upstream LLM call, in milliseconds.'),
        cost_usd: zod
          .number()
          .min(updateEntryResponseUsageOneCostUsdMin)
          .optional()
          .describe('Total estimated cost of this call, in USD.'),
        cost_input_usd: zod
          .number()
          .min(updateEntryResponseUsageOneCostInputUsdMin)
          .optional()
          .describe('Estimated cost attributed to input tokens, in USD.'),
        cost_output_usd: zod
          .number()
          .min(updateEntryResponseUsageOneCostOutputUsdMin)
          .optional()
          .describe('Estimated cost attributed to output tokens, in USD.'),
        input_tokens: zod
          .number()
          .min(updateEntryResponseUsageOneInputTokensMin)
          .optional()
          .describe('Number of input tokens consumed.'),
        output_tokens: zod
          .number()
          .min(updateEntryResponseUsageOneOutputTokensMin)
          .optional()
          .describe('Number of output tokens produced.'),
        cached_tokens: zod
          .number()
          .min(updateEntryResponseUsageOneCachedTokensMin)
          .optional()
          .describe('Number of input tokens served from a prompt cache (subset of input_tokens).'),
      })
      .describe(
        'Structured usage metrics captured at log time.\n\nEvery field is optional so producers can populate whatever they have without\nschema breakage. Stored as the entry-level ``usage`` field so filters can\nreach it via ``data.usage.<field>`` entity-store paths.'
      )
      .optional()
      .describe('Structured usage metrics (model served, latency, cost, token counts).'),
    context: zod
      .object({
        app: zod
          .string()
          .describe(
            "Reference to the app that produced this entry, in the form `workspace\/name`. If the app doesn't exist, it will be automatically created when the entry is ingested."
          ),
        task: zod
          .string()
          .describe(
            "Name of the task within the app (e.g., 'chat', 'completion', 'tool-call'). If the task doesn't exist, it will be automatically created when the entry is ingested."
          ),
        thread_id: zod
          .string()
          .optional()
          .describe(
            'Logical thread identifier that groups related entries in a multi-turn conversation. If provided, entries with the same thread_id are treated as part of the same conversation. If omitted, the entry is treated as a single-turn interaction (e.g., a standalone tool call or completion).'
          ),
        user_id: zod
          .string()
          .optional()
          .describe(
            "Identifier of the application's end-user who triggered this LLM interaction. This represents the person using your application (e.g., 'customer_123', 'employee@company.com'), NOT the service account that created the entry record (see ownership.created_by for that). Use this to track which of your users a conversation belongs to, filter entries by user, and enable per-user analytics. Format is application-defined."
          ),
        trace_id: zod
          .string()
          .optional()
          .describe(
            'Distributed trace identifier (e.g., W3C traceparent). Intake stores it verbatim and does not use it at ingestion time; helps with later cross-system joins.'
          ),
        session_id: zod
          .string()
          .optional()
          .describe(
            'Long-lived session identifier (e.g., user account or browser session). Stored for post-processing analytics; not used by the Intake service at runtime.'
          ),
        created_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the entry was created.'),
      })
      .describe(
        'Contextual metadata attached to every entry record.\n\nKeeping these grouped in a dedicated object avoids polluting the top-level\nentity schema and makes it trivial to extend without breaking compatibility.'
      )
      .describe('Metadata describing producer, task, trace'),
    user_rating: zod
      .object({
        thumb: zod
          .enum(['up', 'down'])
          .describe('Possible thumb feedback choices.')
          .optional()
          .describe(
            'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
          ),
        rating: zod
          .number()
          .min(updateEntryResponseUserRatingOneRatingMin)
          .optional()
          .describe(
            'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
          ),
        opinion: zod
          .string()
          .min(1)
          .max(updateEntryResponseUserRatingOneOpinionMax)
          .optional()
          .describe(
            'Free-text comment from the end user describing their opinion of the response.'
          ),
        rewrite: zod
          .string()
          .min(1)
          .max(updateEntryResponseUserRatingOneRewriteMax)
          .optional()
          .describe(
            "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
          ),
        chosen_index: zod
          .number()
          .min(updateEntryResponseUserRatingOneChosenIndexMin)
          .optional()
          .describe(
            'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
          ),
        categories: zod
          .record(zod.string(), zod.union([zod.number(), zod.string()]))
          .optional()
          .describe(
            "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
          ),
      })
      .describe(
        "User's rating\/evaluation of an AI response.\n\nThis captures various forms of end-user feedback about a model's response, including\nbinary thumbs up\/down ratings, numeric scores, free-text opinions, suggested rewrites,\nand structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfields are optional to accommodate different feedback collection patterns."
      )
      .optional()
      .describe("User's rating\/evaluation"),
    events: zod
      .array(
        zod.union([
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_feedback')
                .default(updateEntryResponseEventsItemOneEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(updateEntryResponseEventsItemOneRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(updateEntryResponseEventsItemOneOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(updateEntryResponseEventsItemOneRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(updateEntryResponseEventsItemOneChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
            })
            .describe(
              "Structured feedback supplied by an end-user.\n\nThis event captures various forms of end-user feedback about a model's response,\nincluding binary thumbs up\/down ratings, numeric scores, free-text opinions,\nsuggested rewrites, and structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfeedback fields are optional to accommodate different feedback collection patterns."
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_action')
                .default(updateEntryResponseEventsItemTwoEventTypeDefault),
              action: zod
                .string()
                .max(updateEntryResponseEventsItemTwoActionMax)
                .describe(
                  "Descriptive name for the action taken by the user (e.g., 'share_clicked', 'code_copied', 'link_followed'). Use snake-case or kebab-case. This is a label, not a unique identifier—multiple events can have the same action name."
                ),
              metadata: zod
                .record(
                  zod.string(),
                  zod.union([
                    zod.string().max(updateEntryResponseEventsItemTwoMetadataOneMax),
                    zod.array(zod.string().max(updateEntryResponseEventsItemTwoMetadataTwoItemMax)),
                    zod.boolean(),
                    zod.number(),
                    zod.number(),
                  ])
                )
                .optional()
                .describe(
                  "Optional key-value pairs with additional context about the action (max 16 entries). Use this for details like user IDs, item IDs, timestamps, A\/B test variants, or any other information useful for downstream training or evaluation pipelines. Example: {'user_id': '12345', 'experiment': 'variant_b', 'item_purchased': 'product_456'}."
                ),
            })
            .describe(
              'Free-form user action captured by the client application.\n\nUse this to track arbitrary user interactions with AI responses, such as copying code,\nclicking share buttons, making purchases, or any other measurable action.\n\nThe action identifier must be ≤256 characters and should use snake-case or kebab-case\nfor consistency (e.g., ``share_clicked``, ``code_copied``, ``purchase_made``).'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('reviewer_annotation')
                .default(updateEntryResponseEventsItemThreeEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(updateEntryResponseEventsItemThreeRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(updateEntryResponseEventsItemThreeOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(updateEntryResponseEventsItemThreeRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(updateEntryResponseEventsItemThreeChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
              response_override: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Complete JSON object that replaces the original model response when exporting data. Unlike the `rewrite` field (which is just text), this can include tool calls, function outputs, and all other response metadata. When an entry with response_override is exported, you can choose to use this corrected response instead of the original. Example: {'choices': [{'message': {'role': 'assistant', 'content': 'Corrected text', 'tool_calls': [...]}}]}"
                ),
            })
            .describe(
              'Structured annotation supplied by a reviewer or expert evaluator.\n\nA reviewer annotation is similar to user feedback but includes an additional capability\nto provide a complete replacement response. This is useful when human experts need to\ncorrect not just the text but also structured elements like tool calls, function outputs,\nor other response metadata.\n\nInherits all feedback fields from UserFeedbackEvent (thumb, rating, opinion, rewrite,\nchosen_index, categories) and adds response_override for full response replacement.'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('evaluator_result')
                .default(updateEntryResponseEventsItemFourEventTypeDefault),
              name: zod
                .string()
                .max(updateEntryResponseEventsItemFourNameMax)
                .describe(
                  "Identifier of the evaluator that produced this result (e.g., 'harbor.verifier', 'evaluator.llm_judge', 'auditor.pii_probe')."
                ),
              score: zod
                .union([zod.number(), zod.number(), zod.string()])
                .optional()
                .describe(
                  "The result value: a number (e.g., reward, rating, probability) or a string (e.g., 'pass', 'fail', a category label). Semantics are defined by the evaluator."
                ),
              metadata: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  'Free-form additional context about this result (e.g., supporting metrics, trial_name, rubric_version, evaluator config snapshot).'
                ),
            })
            .describe(
              'Result produced by an automated evaluator.\n\nUse this for any non-human scorer that emits a judgement about an entry:\neval-framework verifier rewards, LLM-judge ratings, auditor probe results, etc.\nDistinct from UserFeedbackEvent (human end-user feedback) and ReviewerAnnotationEvent\n(human expert annotation).'
            ),
        ])
      )
      .optional()
      .describe('Events associated with this entry'),
    custom_fields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe('Free-form metadata bag for client-defined fields.'),
    created_at: zod.string().datetime({}).optional().describe('Creation timestamp'),
    updated_at: zod.string().datetime({}).optional().describe('Last update timestamp'),
  })
  .describe('Schema for Entry responses.');

/**
 * Delete an entry by ID or external_id.

Use `external:{external_id}` to delete by external_id.
Example: `/v2/workspaces/{workspace}/entries/external:chatcmpl-abc123`
 * @summary Delete Entry
 */
export const DeleteEntryParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

/**
 * Add events to an entry by ID or external_id.

Use `external:{external_id}` to add events by external_id.
Example: `/v2/workspaces/{workspace}/entries/external:chatcmpl-abc123/events`
 * @summary Add Events
 */
export const AddEventsParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

export const addEventsBodyEventsItemOneEventTypeDefault = `user_feedback`;
export const addEventsBodyEventsItemOneRatingMin = 0;

export const addEventsBodyEventsItemOneOpinionMax = 2000;

export const addEventsBodyEventsItemOneRewriteMax = 10000;

export const addEventsBodyEventsItemOneChosenIndexMin = 0;

export const addEventsBodyEventsItemTwoEventTypeDefault = `user_action`;
export const addEventsBodyEventsItemTwoActionMax = 256;

export const addEventsBodyEventsItemTwoMetadataOneMax = 256;

export const addEventsBodyEventsItemTwoMetadataTwoItemMax = 256;

export const addEventsBodyEventsItemThreeEventTypeDefault = `reviewer_annotation`;
export const addEventsBodyEventsItemThreeRatingMin = 0;

export const addEventsBodyEventsItemThreeOpinionMax = 2000;

export const addEventsBodyEventsItemThreeRewriteMax = 10000;

export const addEventsBodyEventsItemThreeChosenIndexMin = 0;

export const addEventsBodyEventsItemFourEventTypeDefault = `evaluator_result`;
export const addEventsBodyEventsItemFourNameMax = 256;

export const AddEventsBody = zod
  .object({
    events: zod
      .array(
        zod.union([
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_feedback')
                .default(addEventsBodyEventsItemOneEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(addEventsBodyEventsItemOneRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(addEventsBodyEventsItemOneOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(addEventsBodyEventsItemOneRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(addEventsBodyEventsItemOneChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
            })
            .describe(
              "Structured feedback supplied by an end-user.\n\nThis event captures various forms of end-user feedback about a model's response,\nincluding binary thumbs up\/down ratings, numeric scores, free-text opinions,\nsuggested rewrites, and structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfeedback fields are optional to accommodate different feedback collection patterns."
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_action')
                .default(addEventsBodyEventsItemTwoEventTypeDefault),
              action: zod
                .string()
                .max(addEventsBodyEventsItemTwoActionMax)
                .describe(
                  "Descriptive name for the action taken by the user (e.g., 'share_clicked', 'code_copied', 'link_followed'). Use snake-case or kebab-case. This is a label, not a unique identifier—multiple events can have the same action name."
                ),
              metadata: zod
                .record(
                  zod.string(),
                  zod.union([
                    zod.string().max(addEventsBodyEventsItemTwoMetadataOneMax),
                    zod.array(zod.string().max(addEventsBodyEventsItemTwoMetadataTwoItemMax)),
                    zod.boolean(),
                    zod.number(),
                    zod.number(),
                  ])
                )
                .optional()
                .describe(
                  "Optional key-value pairs with additional context about the action (max 16 entries). Use this for details like user IDs, item IDs, timestamps, A\/B test variants, or any other information useful for downstream training or evaluation pipelines. Example: {'user_id': '12345', 'experiment': 'variant_b', 'item_purchased': 'product_456'}."
                ),
            })
            .describe(
              'Free-form user action captured by the client application.\n\nUse this to track arbitrary user interactions with AI responses, such as copying code,\nclicking share buttons, making purchases, or any other measurable action.\n\nThe action identifier must be ≤256 characters and should use snake-case or kebab-case\nfor consistency (e.g., ``share_clicked``, ``code_copied``, ``purchase_made``).'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('reviewer_annotation')
                .default(addEventsBodyEventsItemThreeEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(addEventsBodyEventsItemThreeRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(addEventsBodyEventsItemThreeOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(addEventsBodyEventsItemThreeRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(addEventsBodyEventsItemThreeChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
              response_override: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Complete JSON object that replaces the original model response when exporting data. Unlike the `rewrite` field (which is just text), this can include tool calls, function outputs, and all other response metadata. When an entry with response_override is exported, you can choose to use this corrected response instead of the original. Example: {'choices': [{'message': {'role': 'assistant', 'content': 'Corrected text', 'tool_calls': [...]}}]}"
                ),
            })
            .describe(
              'Structured annotation supplied by a reviewer or expert evaluator.\n\nA reviewer annotation is similar to user feedback but includes an additional capability\nto provide a complete replacement response. This is useful when human experts need to\ncorrect not just the text but also structured elements like tool calls, function outputs,\nor other response metadata.\n\nInherits all feedback fields from UserFeedbackEvent (thumb, rating, opinion, rewrite,\nchosen_index, categories) and adds response_override for full response replacement.'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('evaluator_result')
                .default(addEventsBodyEventsItemFourEventTypeDefault),
              name: zod
                .string()
                .max(addEventsBodyEventsItemFourNameMax)
                .describe(
                  "Identifier of the evaluator that produced this result (e.g., 'harbor.verifier', 'evaluator.llm_judge', 'auditor.pii_probe')."
                ),
              score: zod
                .union([zod.number(), zod.number(), zod.string()])
                .optional()
                .describe(
                  "The result value: a number (e.g., reward, rating, probability) or a string (e.g., 'pass', 'fail', a category label). Semantics are defined by the evaluator."
                ),
              metadata: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  'Free-form additional context about this result (e.g., supporting metrics, trial_name, rubric_version, evaluator config snapshot).'
                ),
            })
            .describe(
              'Result produced by an automated evaluator.\n\nUse this for any non-human scorer that emits a judgement about an entry:\neval-framework verifier rewards, LLM-judge ratings, auditor probe results, etc.\nDistinct from UserFeedbackEvent (human end-user feedback) and ReviewerAnnotationEvent\n(human expert annotation).'
            ),
        ])
      )
      .min(1)
      .describe('List of events to add to the entry.'),
  })
  .describe('Request to add events to an entry.');

export const addEventsResponseUsageOneLatencyMsMin = 0;

export const addEventsResponseUsageOneCostUsdMin = 0;

export const addEventsResponseUsageOneCostInputUsdMin = 0;

export const addEventsResponseUsageOneCostOutputUsdMin = 0;

export const addEventsResponseUsageOneInputTokensMin = 0;

export const addEventsResponseUsageOneOutputTokensMin = 0;

export const addEventsResponseUsageOneCachedTokensMin = 0;

export const addEventsResponseUserRatingOneRatingMin = 0;

export const addEventsResponseUserRatingOneOpinionMax = 2000;

export const addEventsResponseUserRatingOneRewriteMax = 10000;

export const addEventsResponseUserRatingOneChosenIndexMin = 0;

export const addEventsResponseEventsItemOneEventTypeDefault = `user_feedback`;
export const addEventsResponseEventsItemOneRatingMin = 0;

export const addEventsResponseEventsItemOneOpinionMax = 2000;

export const addEventsResponseEventsItemOneRewriteMax = 10000;

export const addEventsResponseEventsItemOneChosenIndexMin = 0;

export const addEventsResponseEventsItemTwoEventTypeDefault = `user_action`;
export const addEventsResponseEventsItemTwoActionMax = 256;

export const addEventsResponseEventsItemTwoMetadataOneMax = 256;

export const addEventsResponseEventsItemTwoMetadataTwoItemMax = 256;

export const addEventsResponseEventsItemThreeEventTypeDefault = `reviewer_annotation`;
export const addEventsResponseEventsItemThreeRatingMin = 0;

export const addEventsResponseEventsItemThreeOpinionMax = 2000;

export const addEventsResponseEventsItemThreeRewriteMax = 10000;

export const addEventsResponseEventsItemThreeChosenIndexMin = 0;

export const addEventsResponseEventsItemFourEventTypeDefault = `evaluator_result`;
export const addEventsResponseEventsItemFourNameMax = 256;

export const AddEventsResponse = zod
  .object({
    id: zod.string().describe('Unique identifier'),
    name: zod.string().describe('Entry name (auto-generated)'),
    workspace: zod.string().describe('Workspace identifier'),
    external_id: zod.string().optional().describe('Client-provided identifier'),
    project: zod.string().optional().describe('The name of the project associated with this entry'),
    data: zod
      .object({
        request: zod
          .object({
            messages: zod
              .array(
                zod
                  .object({
                    role: zod
                      .enum(['user', 'system', 'assistant', 'developer', 'tool', 'function'])
                      .describe('Valid role values for entry request messages.')
                      .describe(
                        'The role of the message sender. Must be one of: user, system, assistant, developer, tool, or function.'
                      ),
                  })
                  .describe(
                    'A flexible message model that requires a valid role field but allows any other fields.\n\nThis flexibility enables the Intake service to store messages from various LLM providers\nand future model types without requiring schema updates. Additional fields like `content`,\n`name`, `tool_calls`, `tool_call_id`, etc. are all accepted.\n\nExamples of additional fields:\n- `content`: The message text or content\n- `name`: Name of the user or function\n- `tool_calls`: Tool\/function calls in the message\n- `tool_call_id`: ID of the tool call being responded to'
                  )
              )
              .describe(
                'A list of messages comprising the conversation. Each message must have a valid role. Additional fields like `content`, `tool_calls`, etc. are provider-specific.'
              ),
            model: zod
              .string()
              .describe(
                "The model identifier used for this request (e.g., 'gpt-4', 'llama-3-70b', 'claude-3-opus')."
              ),
          })
          .describe(
            'Flexible entry request that accepts any object shape.\n\nThis flexibility enables the Intake service to store requests from various LLM providers\n(OpenAI, Anthropic, NIM, etc.) and future model types (embeddings, multimodal, etc.)\nwithout requiring schema updates.\n\nRequired fields: `messages` and `model`\nCommon optional fields: `temperature`, `max_tokens`, `top_p`, `tools`, `tool_choice`,\n`stream`, `response_format`, etc.'
          )
          .describe('Raw request payload recorded from the client.'),
        response: zod
          .union([zod.unknown(), zod.unknown()])
          .and(
            zod.object({
              choices: zod
                .array(zod.record(zod.string(), zod.unknown()))
                .optional()
                .describe(
                  'A list of response choices generated by the model. Each choice typically contains a message object with role, content, and optional tool_calls. The structure varies by provider but commonly includes fields like `index`, `message`, and `finish_reason`.'
                ),
              error: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Error envelope returned by the provider when the call failed (e.g., OpenAI's `{message, type, code, param}` shape). Mutually exclusive with `choices`: a response carries one or the other."
                ),
            })
          )
          .describe(
            'Flexible entry response that accepts any object shape.\n\nThis flexibility enables the Intake service to store responses from various LLM providers\nand future model types without requiring schema updates.\n\nRequired: either `choices` (successful response) or `error` (failed call).\nCommon optional fields: `id`, `created`, `model`, `usage`, `system_fingerprint`, etc.'
          )
          .describe('Raw response payload generated by the model.'),
      })
      .describe('Entry data containing the request and response for an LLM interaction.')
      .describe('Entry data containing request and response'),
    usage: zod
      .object({
        model: zod
          .string()
          .optional()
          .describe(
            'The actual model that served the request (after any routing). May differ from the model in the request body.'
          ),
        started_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call started.'),
        ended_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the upstream LLM call ended.'),
        latency_ms: zod
          .number()
          .min(addEventsResponseUsageOneLatencyMsMin)
          .optional()
          .describe('Wall-clock latency of the upstream LLM call, in milliseconds.'),
        cost_usd: zod
          .number()
          .min(addEventsResponseUsageOneCostUsdMin)
          .optional()
          .describe('Total estimated cost of this call, in USD.'),
        cost_input_usd: zod
          .number()
          .min(addEventsResponseUsageOneCostInputUsdMin)
          .optional()
          .describe('Estimated cost attributed to input tokens, in USD.'),
        cost_output_usd: zod
          .number()
          .min(addEventsResponseUsageOneCostOutputUsdMin)
          .optional()
          .describe('Estimated cost attributed to output tokens, in USD.'),
        input_tokens: zod
          .number()
          .min(addEventsResponseUsageOneInputTokensMin)
          .optional()
          .describe('Number of input tokens consumed.'),
        output_tokens: zod
          .number()
          .min(addEventsResponseUsageOneOutputTokensMin)
          .optional()
          .describe('Number of output tokens produced.'),
        cached_tokens: zod
          .number()
          .min(addEventsResponseUsageOneCachedTokensMin)
          .optional()
          .describe('Number of input tokens served from a prompt cache (subset of input_tokens).'),
      })
      .describe(
        'Structured usage metrics captured at log time.\n\nEvery field is optional so producers can populate whatever they have without\nschema breakage. Stored as the entry-level ``usage`` field so filters can\nreach it via ``data.usage.<field>`` entity-store paths.'
      )
      .optional()
      .describe('Structured usage metrics (model served, latency, cost, token counts).'),
    context: zod
      .object({
        app: zod
          .string()
          .describe(
            "Reference to the app that produced this entry, in the form `workspace\/name`. If the app doesn't exist, it will be automatically created when the entry is ingested."
          ),
        task: zod
          .string()
          .describe(
            "Name of the task within the app (e.g., 'chat', 'completion', 'tool-call'). If the task doesn't exist, it will be automatically created when the entry is ingested."
          ),
        thread_id: zod
          .string()
          .optional()
          .describe(
            'Logical thread identifier that groups related entries in a multi-turn conversation. If provided, entries with the same thread_id are treated as part of the same conversation. If omitted, the entry is treated as a single-turn interaction (e.g., a standalone tool call or completion).'
          ),
        user_id: zod
          .string()
          .optional()
          .describe(
            "Identifier of the application's end-user who triggered this LLM interaction. This represents the person using your application (e.g., 'customer_123', 'employee@company.com'), NOT the service account that created the entry record (see ownership.created_by for that). Use this to track which of your users a conversation belongs to, filter entries by user, and enable per-user analytics. Format is application-defined."
          ),
        trace_id: zod
          .string()
          .optional()
          .describe(
            'Distributed trace identifier (e.g., W3C traceparent). Intake stores it verbatim and does not use it at ingestion time; helps with later cross-system joins.'
          ),
        session_id: zod
          .string()
          .optional()
          .describe(
            'Long-lived session identifier (e.g., user account or browser session). Stored for post-processing analytics; not used by the Intake service at runtime.'
          ),
        created_at: zod
          .string()
          .datetime({})
          .optional()
          .describe('UTC timestamp when the entry was created.'),
      })
      .describe(
        'Contextual metadata attached to every entry record.\n\nKeeping these grouped in a dedicated object avoids polluting the top-level\nentity schema and makes it trivial to extend without breaking compatibility.'
      )
      .describe('Metadata describing producer, task, trace'),
    user_rating: zod
      .object({
        thumb: zod
          .enum(['up', 'down'])
          .describe('Possible thumb feedback choices.')
          .optional()
          .describe(
            'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
          ),
        rating: zod
          .number()
          .min(addEventsResponseUserRatingOneRatingMin)
          .optional()
          .describe(
            'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
          ),
        opinion: zod
          .string()
          .min(1)
          .max(addEventsResponseUserRatingOneOpinionMax)
          .optional()
          .describe(
            'Free-text comment from the end user describing their opinion of the response.'
          ),
        rewrite: zod
          .string()
          .min(1)
          .max(addEventsResponseUserRatingOneRewriteMax)
          .optional()
          .describe(
            "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
          ),
        chosen_index: zod
          .number()
          .min(addEventsResponseUserRatingOneChosenIndexMin)
          .optional()
          .describe(
            'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
          ),
        categories: zod
          .record(zod.string(), zod.union([zod.number(), zod.string()]))
          .optional()
          .describe(
            "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
          ),
      })
      .describe(
        "User's rating\/evaluation of an AI response.\n\nThis captures various forms of end-user feedback about a model's response, including\nbinary thumbs up\/down ratings, numeric scores, free-text opinions, suggested rewrites,\nand structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfields are optional to accommodate different feedback collection patterns."
      )
      .optional()
      .describe("User's rating\/evaluation"),
    events: zod
      .array(
        zod.union([
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_feedback')
                .default(addEventsResponseEventsItemOneEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(addEventsResponseEventsItemOneRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(addEventsResponseEventsItemOneOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(addEventsResponseEventsItemOneRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(addEventsResponseEventsItemOneChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
            })
            .describe(
              "Structured feedback supplied by an end-user.\n\nThis event captures various forms of end-user feedback about a model's response,\nincluding binary thumbs up\/down ratings, numeric scores, free-text opinions,\nsuggested rewrites, and structured category ratings.\n\nEither `thumb` or `rating` should be provided (they are mutually exclusive), but all\nfeedback fields are optional to accommodate different feedback collection patterns."
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('user_action')
                .default(addEventsResponseEventsItemTwoEventTypeDefault),
              action: zod
                .string()
                .max(addEventsResponseEventsItemTwoActionMax)
                .describe(
                  "Descriptive name for the action taken by the user (e.g., 'share_clicked', 'code_copied', 'link_followed'). Use snake-case or kebab-case. This is a label, not a unique identifier—multiple events can have the same action name."
                ),
              metadata: zod
                .record(
                  zod.string(),
                  zod.union([
                    zod.string().max(addEventsResponseEventsItemTwoMetadataOneMax),
                    zod.array(zod.string().max(addEventsResponseEventsItemTwoMetadataTwoItemMax)),
                    zod.boolean(),
                    zod.number(),
                    zod.number(),
                  ])
                )
                .optional()
                .describe(
                  "Optional key-value pairs with additional context about the action (max 16 entries). Use this for details like user IDs, item IDs, timestamps, A\/B test variants, or any other information useful for downstream training or evaluation pipelines. Example: {'user_id': '12345', 'experiment': 'variant_b', 'item_purchased': 'product_456'}."
                ),
            })
            .describe(
              'Free-form user action captured by the client application.\n\nUse this to track arbitrary user interactions with AI responses, such as copying code,\nclicking share buttons, making purchases, or any other measurable action.\n\nThe action identifier must be ≤256 characters and should use snake-case or kebab-case\nfor consistency (e.g., ``share_clicked``, ``code_copied``, ``purchase_made``).'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('reviewer_annotation')
                .default(addEventsResponseEventsItemThreeEventTypeDefault),
              thumb: zod
                .enum(['up', 'down'])
                .describe('Possible thumb feedback choices.')
                .optional()
                .describe(
                  'Binary feedback: \"up\" for 👍 or \"down\" for 👎. Mutually exclusive with `rating`. Use this for simple thumbs up\/down UI elements.'
                ),
              rating: zod
                .number()
                .min(addEventsResponseEventsItemThreeRatingMin)
                .optional()
                .describe(
                  'Numeric rating (e.g., 1-5 stars) provided by the end user. Mutually exclusive with `thumb`. Use this for star ratings or numeric scales.'
                ),
              opinion: zod
                .string()
                .min(1)
                .max(addEventsResponseEventsItemThreeOpinionMax)
                .optional()
                .describe(
                  'Free-text comment from the end user describing their opinion of the response.'
                ),
              rewrite: zod
                .string()
                .min(1)
                .max(addEventsResponseEventsItemThreeRewriteMax)
                .optional()
                .describe(
                  "End-user's suggested text replacement for the generated response. This is the user's idea of what the response should have been."
                ),
              chosen_index: zod
                .number()
                .min(addEventsResponseEventsItemThreeChosenIndexMin)
                .optional()
                .describe(
                  'Zero-based index of the response option the user selected when multiple responses were returned. Use this when showing users multiple completion choices and tracking which one they picked.'
                ),
              categories: zod
                .record(zod.string(), zod.union([zod.number(), zod.string()]))
                .optional()
                .describe(
                  "Application-specific category ratings as key-value pairs. Use this for custom rating dimensions (e.g., {'helpfulness': 4, 'accuracy': 5, 'tone': 'professional'}). Useful for radio buttons, dropdowns, or multi-dimensional rating systems."
                ),
              response_override: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  "Complete JSON object that replaces the original model response when exporting data. Unlike the `rewrite` field (which is just text), this can include tool calls, function outputs, and all other response metadata. When an entry with response_override is exported, you can choose to use this corrected response instead of the original. Example: {'choices': [{'message': {'role': 'assistant', 'content': 'Corrected text', 'tool_calls': [...]}}]}"
                ),
            })
            .describe(
              'Structured annotation supplied by a reviewer or expert evaluator.\n\nA reviewer annotation is similar to user feedback but includes an additional capability\nto provide a complete replacement response. This is useful when human experts need to\ncorrect not just the text but also structured elements like tool calls, function outputs,\nor other response metadata.\n\nInherits all feedback fields from UserFeedbackEvent (thumb, rating, opinion, rewrite,\nchosen_index, categories) and adds response_override for full response replacement.'
            ),
          zod
            .object({
              id: zod
                .string()
                .optional()
                .describe(
                  'Unique identifier for the event. Populated when retrieved from database.'
                ),
              created_at: zod
                .string()
                .datetime({})
                .optional()
                .describe('UTC timestamp when the record was created.'),
              created_by: zod
                .record(zod.string(), zod.string())
                .optional()
                .describe(
                  'Identifier of the user or system that generated the record. Can be set of key-value pairs.'
                ),
              event_type: zod
                .literal('evaluator_result')
                .default(addEventsResponseEventsItemFourEventTypeDefault),
              name: zod
                .string()
                .max(addEventsResponseEventsItemFourNameMax)
                .describe(
                  "Identifier of the evaluator that produced this result (e.g., 'harbor.verifier', 'evaluator.llm_judge', 'auditor.pii_probe')."
                ),
              score: zod
                .union([zod.number(), zod.number(), zod.string()])
                .optional()
                .describe(
                  "The result value: a number (e.g., reward, rating, probability) or a string (e.g., 'pass', 'fail', a category label). Semantics are defined by the evaluator."
                ),
              metadata: zod
                .record(zod.string(), zod.unknown())
                .optional()
                .describe(
                  'Free-form additional context about this result (e.g., supporting metrics, trial_name, rubric_version, evaluator config snapshot).'
                ),
            })
            .describe(
              'Result produced by an automated evaluator.\n\nUse this for any non-human scorer that emits a judgement about an entry:\neval-framework verifier rewards, LLM-judge ratings, auditor probe results, etc.\nDistinct from UserFeedbackEvent (human end-user feedback) and ReviewerAnnotationEvent\n(human expert annotation).'
            ),
        ])
      )
      .optional()
      .describe('Events associated with this entry'),
    custom_fields: zod
      .record(zod.string(), zod.unknown())
      .optional()
      .describe('Free-form metadata bag for client-defined fields.'),
    created_at: zod.string().datetime({}).optional().describe('Creation timestamp'),
    updated_at: zod.string().datetime({}).optional().describe('Last update timestamp'),
  })
  .describe('Schema for Entry responses.');
