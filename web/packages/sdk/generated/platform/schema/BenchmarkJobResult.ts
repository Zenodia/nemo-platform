/**
 * SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by Orval 🍺
 * Do not edit manually.
 * Nemo Platform API
 */
import type { BenchmarkJobResultLabels } from './BenchmarkJobResultLabels';
import type { BenchmarkMetricResult } from './BenchmarkMetricResult';
import type { BenchmarkRef } from './BenchmarkRef';
import type { FilesetRef } from './FilesetRef';
import type { MetricRef } from './MetricRef';
import type { ModelRef } from './ModelRef';

/**
 * Response type for benchmark job result.
 */
export interface BenchmarkJobResult {
  /** Entity name within the workspace */
  name?: string;
  /**
   * Workspace identifier
   * @pattern ^[\w\-\+.@:]+$
   */
  workspace: string;
  /** The name of the project associated with this entity. */
  project?: string;
  /** The dataset used for the evaluation job to generate the result. This field is only populated when the job specifies a FilesetRef. */
  dataset?: FilesetRef;
  /** The model evaluated for the job to generate the result. This field is only populated when the job specifies a ModelRef. */
  model?: ModelRef;
  /** Labels are key-value pairs that can be used for grouping and filtering. */
  labels?: BenchmarkJobResultLabels;
  /** The benchmark used for the evaluation job to generate the result. */
  benchmark: BenchmarkRef;
  /** The list of metrics used for the evaluation job to generate the result. */
  metrics?: MetricRef[];
  /** Results for each metric in the benchmark. */
  results: BenchmarkMetricResult[];
  readonly id: string;
  readonly created_at: string;
  readonly created_by: string | null;
  readonly updated_at: string;
  readonly updated_by: string | null;
  /** Alias for id for backwards compatibility. */
  readonly entity_id: string;
  /** Parent entity ID for nested entities. */
  readonly parent: string;
}
