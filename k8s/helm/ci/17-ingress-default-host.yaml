# CI validates by running: helm template nemo-platform . -f ci/17-ingress-default-host.yaml
# Tests that setting ingress.defaultHost renders one Ingress rule with that host and default paths (backend is API service).

ingress:
  enabled: true
  defaultHost: "nmp.example.com"

