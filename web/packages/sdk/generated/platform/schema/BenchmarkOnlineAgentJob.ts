/**
 * SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by Orval 🍺
 * Do not edit manually.
 * Nemo Platform API
 */
import type { Agent } from './Agent';
import type { BenchmarkOnlineAgentJobPromptTemplate } from './BenchmarkOnlineAgentJobPromptTemplate';
import type { BenchmarkRef } from './BenchmarkRef';
import type { RunConfigOnline } from './RunConfigOnline';

/**
 * Input for an online benchmark evaluation job targeting an agent.

Evaluates an agent by prompting it with the benchmark's dataset and then evaluating
the responses against all metrics in the benchmark.
 */
export interface BenchmarkOnlineAgentJob {
  /** Reference to the benchmark for evaluation (format: workspace/name). */
  benchmark: BenchmarkRef;
  /** The agent to evaluate. */
  agent: Agent;
  /** Execution parameters for the benchmark job. */
  params?: RunConfigOnline;
  /** The jinja template to prompt the agent for evaluation. Can be either a simple string or a structured object (e.g., OpenAI messages format). Use Jinja template variables like {{input}}, {{output}}, {{context}}, {{reference}} to reference input columns. */
  prompt_template: BenchmarkOnlineAgentJobPromptTemplate;
  /** Prompt template fields that should remain available to the prompt template but not be required by dataset schema validation. */
  optional_fields?: string[];
}
