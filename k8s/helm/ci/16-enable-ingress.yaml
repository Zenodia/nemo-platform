# CI validates by running: helm template nemo-platform . -f ci/16-enable-ingress.yaml
# Tests that enabling ingress renders a valid Ingress resource (path-only by default).

ingress:
  enabled: true

