/**
 * SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by Orval 🍺
 * Do not edit manually.
 * Nemo Platform API
 */
import * as zod from 'zod';

/**
 * Create a new VirtualModel in the given workspace.

A VirtualModel defines an ordered middleware pipeline that IGW executes
when an inference request arrives with ``model: "workspace/name"`` matching
this entity.
 * @summary Create VirtualModel
 */
export const CreateVirtualModelParams = zod.object({
  workspace: zod.string(),
});

export const createVirtualModelBodyAutoprovisionedDefault = false;

export const CreateVirtualModelBody = zod
  .object({
    default_model_entity: zod
      .string()
      .optional()
      .describe(
        'Model entity to route to, in \"workspace\/name\" format. Written into request[\"model\"] before the request middleware pipeline runs. If omitted, a request middleware plugin must handle backend routing itself. Set to null to clear an existing value.'
      ),
    autoprovisioned: zod
      .boolean()
      .default(createVirtualModelBodyAutoprovisionedDefault)
      .describe(
        'Marks this VirtualModel as controller-managed. The Models controller will delete it once no ModelProvider serves the matching entity. Setting this manually opts the VirtualModel into that cleanup behavior.'
      ),
    models: zod
      .array(
        zod
          .object({
            model: zod.string(),
            backend_format: zod
              .enum(['OPENAI_CHAT', 'ANTHROPIC_MESSAGES'])
              .describe(
                'Inference backend API wire formats understood by IGW and middleware plugins.'
              )
              .nullish()
              .describe('Optional backend format override for this VirtualModel entry.'),
          })
          .describe('Inference configuration for one model entity referenced by a VirtualModel.')
      )
      .optional()
      .describe(
        'Model entity references used by this VirtualModel. A per-entry backend_format overrides the referenced ModelEntity backend_format when IGW resolves the backend format for a request.'
      ),
    request_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .optional()
      .describe(
        'Ordered list of middleware plugins applied before proxying to the backend. Each entry is a MiddlewareCall with a \"name\" (plugin identifier) and optional \"config_type\" and \"config_id\" fields that reference a stored plugin configuration.'
      ),
    response_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .optional()
      .describe(
        'Ordered list of middleware plugins applied after the backend response is received, before returning it to the caller.'
      ),
    post_response_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .optional()
      .describe(
        'Ordered list of middleware plugins invoked after the response has been returned to the caller. Intended for fire-and-forget work (logging, analytics) that must not block or modify the response.'
      ),
    override_proxy: zod
      .string()
      .optional()
      .describe(
        'Plugin-provided proxy implementation for IGW to use instead of its default aiohttp proxy. Format: \"plugin-name.proxy-name\". Leave unset to use the default IGW proxy. Set to null to clear an existing value.'
      ),
    name: zod
      .string()
      .describe('Name of the virtual model within the workspace. Must be unique per workspace.'),
  })
  .describe('Request body for creating a new VirtualModel.');

/**
 * List VirtualModels for the given workspace.

Use ``workspace=-`` to list across all workspaces accessible to the caller.
 * @summary List VirtualModels
 */
export const ListVirtualModelsParams = zod.object({
  workspace: zod.string(),
});

export const listVirtualModelsQueryPageDefault = 1;

export const listVirtualModelsQueryPageSizeDefault = 20;
export const listVirtualModelsQueryPageSizeMax = 200;

export const listVirtualModelsQuerySortDefault = `-created_at`;

export const ListVirtualModelsQueryParams = zod.object({
  page: zod
    .number()
    .min(1)
    .default(listVirtualModelsQueryPageDefault)
    .describe('Page number (1-indexed).'),
  page_size: zod
    .number()
    .min(1)
    .max(listVirtualModelsQueryPageSizeMax)
    .default(listVirtualModelsQueryPageSizeDefault)
    .describe('Number of results per page.'),
  sort: zod
    .string()
    .default(listVirtualModelsQuerySortDefault)
    .describe('Sort field.  Prefix with ``-`` for descending order.'),
});

export const listVirtualModelsResponseDataItemNameDefault = ``;
export const listVirtualModelsResponseDataItemWorkspaceRegExp = new RegExp('^[\\w\\-\\+.@:]+$');
export const listVirtualModelsResponseDataItemAutoprovisionedDefault = false;
export const listVirtualModelsResponseDataItemRequestMiddlewareDefault = [];
export const listVirtualModelsResponseDataItemResponseMiddlewareDefault = [];
export const listVirtualModelsResponseDataItemPostResponseMiddlewareDefault = [];

export const ListVirtualModelsResponse = zod.object({
  data: zod.array(
    zod
      .object({
        name: zod
          .string()
          .default(listVirtualModelsResponseDataItemNameDefault)
          .describe('Entity name within the workspace'),
        workspace: zod
          .string()
          .regex(listVirtualModelsResponseDataItemWorkspaceRegExp)
          .describe('Workspace identifier'),
        project: zod
          .string()
          .optional()
          .describe('The name of the project associated with this entity.'),
        default_model_entity: zod.string().optional(),
        autoprovisioned: zod
          .boolean()
          .default(listVirtualModelsResponseDataItemAutoprovisionedDefault)
          .describe(
            'Marks this VirtualModel as controller-managed. The Models controller will delete it once no ModelProvider serves the matching entity. Setting this manually opts the VirtualModel into that cleanup behavior.'
          ),
        models: zod
          .array(
            zod
              .object({
                model: zod.string(),
                backend_format: zod
                  .enum(['OPENAI_CHAT', 'ANTHROPIC_MESSAGES'])
                  .describe(
                    'Inference backend API wire formats understood by IGW and middleware plugins.'
                  )
                  .nullish()
                  .describe('Optional backend format override for this VirtualModel entry.'),
              })
              .describe(
                'Inference configuration for one model entity referenced by a VirtualModel.'
              )
          )
          .optional(),
        request_middleware: zod
          .array(
            zod
              .object({
                name: zod.string(),
                config_type: zod.string(),
                config: zod.record(zod.string(), zod.unknown()).optional(),
                config_id: zod.string().optional(),
              })
              .describe(
                'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
              )
          )
          .default(listVirtualModelsResponseDataItemRequestMiddlewareDefault),
        response_middleware: zod
          .array(
            zod
              .object({
                name: zod.string(),
                config_type: zod.string(),
                config: zod.record(zod.string(), zod.unknown()).optional(),
                config_id: zod.string().optional(),
              })
              .describe(
                'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
              )
          )
          .default(listVirtualModelsResponseDataItemResponseMiddlewareDefault),
        post_response_middleware: zod
          .array(
            zod
              .object({
                name: zod.string(),
                config_type: zod.string(),
                config: zod.record(zod.string(), zod.unknown()).optional(),
                config_id: zod.string().optional(),
              })
              .describe(
                'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
              )
          )
          .default(listVirtualModelsResponseDataItemPostResponseMiddlewareDefault),
        override_proxy: zod.string().optional(),
        id: zod.string(),
        created_at: zod.string().datetime({}),
        created_by: zod.string().nullable(),
        updated_at: zod.string().datetime({}),
        updated_by: zod.string().nullable(),
        entity_id: zod.string().describe('Alias for id for backwards compatibility.'),
        parent: zod.string().describe('Parent entity ID for nested entities.'),
      })
      .describe(
        'Logical inference route.\n\nMaps a user-facing model name to an optional default model entity and\ndefines ordered middleware pipelines for the request, response, and\npost-response phases.\n\nWhen a caller sets ``model: \"workspace\/my-virtual-model\"`` in an inference\nrequest, IGW resolves the ``VirtualModel`` instead of a ``ModelEntity``\ndirectly. If ``default_model_entity`` is set, IGW writes it into\n``request[\"model\"]`` before the request middleware pipeline runs. Middleware\nmay mutate ``request[\"model\"]`` freely. After the pipeline completes, IGW\nreads ``request[\"model\"]``, resolves it to a ``ModelProvider`` via the\n``ModelCache``, and proxies.\n\nThe ``ModelProviderReconciler`` auto-creates a passthrough ``VirtualModel``\nfor each discovered model (same workspace and name as the ``ModelEntity``,\nempty middleware lists, ``default_model_entity`` pointing to that entity).\nAll existing inference requests continue to work without changes.'
      )
  ),
  pagination: zod
    .object({
      page: zod.number().describe('The current page number.'),
      page_size: zod.number().describe('The page size used for the query.'),
      current_page_size: zod.number().describe('The size for the current page.'),
      total_pages: zod.number().describe('The total number of pages.'),
      total_results: zod.number().describe('The total number of results.'),
    })
    .optional()
    .describe('Pagination information.'),
  sort: zod.string().optional().describe('The field on which the results are sorted.'),
  filter: zod.record(zod.string(), zod.unknown()).optional().describe('Filtering information.'),
});

/**
 * Get a VirtualModel by workspace and name.
 * @summary Get VirtualModel
 */
export const GetVirtualModelParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

export const getVirtualModelResponseNameDefault = ``;
export const getVirtualModelResponseWorkspaceRegExp = new RegExp('^[\\w\\-\\+.@:]+$');
export const getVirtualModelResponseAutoprovisionedDefault = false;
export const getVirtualModelResponseRequestMiddlewareDefault = [];
export const getVirtualModelResponseResponseMiddlewareDefault = [];
export const getVirtualModelResponsePostResponseMiddlewareDefault = [];

export const GetVirtualModelResponse = zod
  .object({
    name: zod
      .string()
      .default(getVirtualModelResponseNameDefault)
      .describe('Entity name within the workspace'),
    workspace: zod
      .string()
      .regex(getVirtualModelResponseWorkspaceRegExp)
      .describe('Workspace identifier'),
    project: zod
      .string()
      .optional()
      .describe('The name of the project associated with this entity.'),
    default_model_entity: zod.string().optional(),
    autoprovisioned: zod
      .boolean()
      .default(getVirtualModelResponseAutoprovisionedDefault)
      .describe(
        'Marks this VirtualModel as controller-managed. The Models controller will delete it once no ModelProvider serves the matching entity. Setting this manually opts the VirtualModel into that cleanup behavior.'
      ),
    models: zod
      .array(
        zod
          .object({
            model: zod.string(),
            backend_format: zod
              .enum(['OPENAI_CHAT', 'ANTHROPIC_MESSAGES'])
              .describe(
                'Inference backend API wire formats understood by IGW and middleware plugins.'
              )
              .nullish()
              .describe('Optional backend format override for this VirtualModel entry.'),
          })
          .describe('Inference configuration for one model entity referenced by a VirtualModel.')
      )
      .optional(),
    request_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .default(getVirtualModelResponseRequestMiddlewareDefault),
    response_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .default(getVirtualModelResponseResponseMiddlewareDefault),
    post_response_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .default(getVirtualModelResponsePostResponseMiddlewareDefault),
    override_proxy: zod.string().optional(),
    id: zod.string(),
    created_at: zod.string().datetime({}),
    created_by: zod.string().nullable(),
    updated_at: zod.string().datetime({}),
    updated_by: zod.string().nullable(),
    entity_id: zod.string().describe('Alias for id for backwards compatibility.'),
    parent: zod.string().describe('Parent entity ID for nested entities.'),
  })
  .describe(
    'Logical inference route.\n\nMaps a user-facing model name to an optional default model entity and\ndefines ordered middleware pipelines for the request, response, and\npost-response phases.\n\nWhen a caller sets ``model: \"workspace\/my-virtual-model\"`` in an inference\nrequest, IGW resolves the ``VirtualModel`` instead of a ``ModelEntity``\ndirectly. If ``default_model_entity`` is set, IGW writes it into\n``request[\"model\"]`` before the request middleware pipeline runs. Middleware\nmay mutate ``request[\"model\"]`` freely. After the pipeline completes, IGW\nreads ``request[\"model\"]``, resolves it to a ``ModelProvider`` via the\n``ModelCache``, and proxies.\n\nThe ``ModelProviderReconciler`` auto-creates a passthrough ``VirtualModel``\nfor each discovered model (same workspace and name as the ``ModelEntity``,\nempty middleware lists, ``default_model_entity`` pointing to that entity).\nAll existing inference requests continue to work without changes.'
  );

/**
 * Partially update a VirtualModel.

Only fields present in the request body are modified.  Fields absent from
the request body retain their current values.
 * @summary Update VirtualModel
 */
export const UpdateVirtualModelParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

export const updateVirtualModelBodyAutoprovisionedDefault = false;

export const UpdateVirtualModelBody = zod
  .object({
    default_model_entity: zod
      .string()
      .optional()
      .describe(
        'Model entity to route to, in \"workspace\/name\" format. Written into request[\"model\"] before the request middleware pipeline runs. If omitted, a request middleware plugin must handle backend routing itself. Set to null to clear an existing value.'
      ),
    autoprovisioned: zod
      .boolean()
      .default(updateVirtualModelBodyAutoprovisionedDefault)
      .describe(
        'Marks this VirtualModel as controller-managed. The Models controller will delete it once no ModelProvider serves the matching entity. Setting this manually opts the VirtualModel into that cleanup behavior.'
      ),
    models: zod
      .array(
        zod
          .object({
            model: zod.string(),
            backend_format: zod
              .enum(['OPENAI_CHAT', 'ANTHROPIC_MESSAGES'])
              .describe(
                'Inference backend API wire formats understood by IGW and middleware plugins.'
              )
              .nullish()
              .describe('Optional backend format override for this VirtualModel entry.'),
          })
          .describe('Inference configuration for one model entity referenced by a VirtualModel.')
      )
      .optional()
      .describe(
        'Model entity references used by this VirtualModel. A per-entry backend_format overrides the referenced ModelEntity backend_format when IGW resolves the backend format for a request.'
      ),
    request_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .optional()
      .describe(
        'Ordered list of middleware plugins applied before proxying to the backend. Each entry is a MiddlewareCall with a \"name\" (plugin identifier) and optional \"config_type\" and \"config_id\" fields that reference a stored plugin configuration.'
      ),
    response_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .optional()
      .describe(
        'Ordered list of middleware plugins applied after the backend response is received, before returning it to the caller.'
      ),
    post_response_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .optional()
      .describe(
        'Ordered list of middleware plugins invoked after the response has been returned to the caller. Intended for fire-and-forget work (logging, analytics) that must not block or modify the response.'
      ),
    override_proxy: zod
      .string()
      .optional()
      .describe(
        'Plugin-provided proxy implementation for IGW to use instead of its default aiohttp proxy. Format: \"plugin-name.proxy-name\". Leave unset to use the default IGW proxy. Set to null to clear an existing value.'
      ),
  })
  .describe(
    'Request body for partially updating an existing VirtualModel (PATCH).\n\nOnly fields present in the request body are updated.  Omitted fields\nretain their current values.  ``model_fields_set`` is used in the handler\nto distinguish an intentional ``[]`` (clear the list) from a missing field\n(leave unchanged).  Set ``default_model_entity`` or ``override_proxy`` to\n``null`` explicitly to clear them.'
  );

export const updateVirtualModelResponseNameDefault = ``;
export const updateVirtualModelResponseWorkspaceRegExp = new RegExp('^[\\w\\-\\+.@:]+$');
export const updateVirtualModelResponseAutoprovisionedDefault = false;
export const updateVirtualModelResponseRequestMiddlewareDefault = [];
export const updateVirtualModelResponseResponseMiddlewareDefault = [];
export const updateVirtualModelResponsePostResponseMiddlewareDefault = [];

export const UpdateVirtualModelResponse = zod
  .object({
    name: zod
      .string()
      .default(updateVirtualModelResponseNameDefault)
      .describe('Entity name within the workspace'),
    workspace: zod
      .string()
      .regex(updateVirtualModelResponseWorkspaceRegExp)
      .describe('Workspace identifier'),
    project: zod
      .string()
      .optional()
      .describe('The name of the project associated with this entity.'),
    default_model_entity: zod.string().optional(),
    autoprovisioned: zod
      .boolean()
      .default(updateVirtualModelResponseAutoprovisionedDefault)
      .describe(
        'Marks this VirtualModel as controller-managed. The Models controller will delete it once no ModelProvider serves the matching entity. Setting this manually opts the VirtualModel into that cleanup behavior.'
      ),
    models: zod
      .array(
        zod
          .object({
            model: zod.string(),
            backend_format: zod
              .enum(['OPENAI_CHAT', 'ANTHROPIC_MESSAGES'])
              .describe(
                'Inference backend API wire formats understood by IGW and middleware plugins.'
              )
              .nullish()
              .describe('Optional backend format override for this VirtualModel entry.'),
          })
          .describe('Inference configuration for one model entity referenced by a VirtualModel.')
      )
      .optional(),
    request_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .default(updateVirtualModelResponseRequestMiddlewareDefault),
    response_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .default(updateVirtualModelResponseResponseMiddlewareDefault),
    post_response_middleware: zod
      .array(
        zod
          .object({
            name: zod.string(),
            config_type: zod.string(),
            config: zod.record(zod.string(), zod.unknown()).optional(),
            config_id: zod.string().optional(),
          })
          .describe(
            'One entry in a VirtualModel middleware pipeline.\n\nDeclares which plugin to invoke and how to resolve its configuration.\nExactly one of ``config`` (inline dict) or ``config_id`` (entity reference)\nshould be provided. ``config_type`` is always required regardless of which\nis used — it is the discriminator that tells IGW (and the plugin) which\nconfig schema applies.\n\nAttributes:\n    name: The entry-point key of the plugin to invoke\n        (e.g. ``\"nemo-switchyard\"``). Must match the plugin\'s\n        ``nemo.inference_middleware`` entry-point key.\n    config_type: Always required. Maps to the ``entity_type`` of the plugin\'s\n        config ``NemoEntity`` subclass (e.g. ``\"routellm_config\"``). Used by\n        IGW to call :meth:`~NemoInferenceMiddleware.validate_middleware_config`\n        with the right discriminator, and by the plugin to dispatch to the\n        correct schema when it supports multiple config types.\n    config: Inline config dict. Mutually exclusive with ``config_id``.\n    config_id: ``\"workspace\/name\"`` reference to a stored config entity.\n        Mutually exclusive with ``config``. IGW resolves this by calling\n        :meth:`~NemoInferenceMiddleware.get_middleware_config` on the plugin.'
          )
      )
      .default(updateVirtualModelResponsePostResponseMiddlewareDefault),
    override_proxy: zod.string().optional(),
    id: zod.string(),
    created_at: zod.string().datetime({}),
    created_by: zod.string().nullable(),
    updated_at: zod.string().datetime({}),
    updated_by: zod.string().nullable(),
    entity_id: zod.string().describe('Alias for id for backwards compatibility.'),
    parent: zod.string().describe('Parent entity ID for nested entities.'),
  })
  .describe(
    'Logical inference route.\n\nMaps a user-facing model name to an optional default model entity and\ndefines ordered middleware pipelines for the request, response, and\npost-response phases.\n\nWhen a caller sets ``model: \"workspace\/my-virtual-model\"`` in an inference\nrequest, IGW resolves the ``VirtualModel`` instead of a ``ModelEntity``\ndirectly. If ``default_model_entity`` is set, IGW writes it into\n``request[\"model\"]`` before the request middleware pipeline runs. Middleware\nmay mutate ``request[\"model\"]`` freely. After the pipeline completes, IGW\nreads ``request[\"model\"]``, resolves it to a ``ModelProvider`` via the\n``ModelCache``, and proxies.\n\nThe ``ModelProviderReconciler`` auto-creates a passthrough ``VirtualModel``\nfor each discovered model (same workspace and name as the ``ModelEntity``,\nempty middleware lists, ``default_model_entity`` pointing to that entity).\nAll existing inference requests continue to work without changes.'
  );

/**
 * Permanently delete a VirtualModel.

This does not affect any in-flight requests already being routed through
this VirtualModel.  IGW's model cache is refreshed on its next polling cycle.
 * @summary Delete VirtualModel
 */
export const DeleteVirtualModelParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});
