/**
 * SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 * SPDX-License-Identifier: Apache-2.0
 *
 * Generated by Orval 🍺
 * Do not edit manually.
 * Nemo Platform API
 */
import * as zod from 'zod';

/**
 * Create an adapter under a base model specified by the "model" field in the body.
 * @summary Create Adapter
 */
export const ModelsCreateAdapterParams = zod.object({
  workspace: zod.string(),
});

export const modelsCreateAdapterBodyNameMax = 255;

export const modelsCreateAdapterBodyNameRegExp = new RegExp('^[\\w\\-.]+$');
export const modelsCreateAdapterBodyDescriptionMax = 1000;

export const modelsCreateAdapterBodyEnabledDefault = true;
export const modelsCreateAdapterBodyModelMax = 127;

export const ModelsCreateAdapterBody = zod
  .object({
    name: zod
      .string()
      .max(modelsCreateAdapterBodyNameMax)
      .regex(modelsCreateAdapterBodyNameRegExp)
      .describe(
        'Name of the adapter. Name must be unique in the workspace. Allowed characters: letters (a-z, A-Z), digits (0-9), underscores, hyphens, and dots.'
      ),
    description: zod
      .string()
      .max(modelsCreateAdapterBodyDescriptionMax)
      .optional()
      .describe('Optional description of the adapter'),
    fileset: zod
      .string()
      .describe(
        'Location where adapter files are stored - expected format {workspace}\/{fileset_name}'
      ),
    finetuning_type: zod
      .enum([
        'lora_merged',
        'all_weights',
        'last_layer',
        'top_layers',
        'gradual_unfreezing',
        'bias_only',
        'attention_only',
        'lora',
        'qlora',
        'adalora',
        'dora',
        'lora_plus',
        'prompt_tuning',
        'prefix_tuning',
        'p_tuning',
        'p_tuning_v2',
        'soft_prompt',
        'ppo',
        'dpo',
        'cdpo',
        'ipo',
        'orpo',
        'kto',
        'rrhf',
        'grpo',
      ])
      .describe('Finetuning types.')
      .describe('Type of finetuning (LORA, P_TUNING, etc.)'),
    enabled: zod
      .boolean()
      .default(modelsCreateAdapterBodyEnabledDefault)
      .describe('Whether to make this adapter available for inference post training'),
    lora_config: zod
      .object({
        alpha: zod.number().optional().describe('Alpha scaling used for this adapter'),
        rank: zod.number().describe('LoRA Rank'),
      })
      .optional()
      .describe('Lora configuration specifics'),
    model: zod
      .string()
      .max(modelsCreateAdapterBodyModelMax)
      .describe(
        "Base model entity.\n            Use `{workspace}\/{model_name}` to reference a model in any workspace, or a single `{model_name}` resolved in the path workspace. A single name (2-63 characters) or 'workspace\/model_name' where each segment is a valid name (lowercase, digits, hyphens, and temporarily @ . + _; no leading\/trailing or consecutive hyphens). If one slash, both sides must be non-empty."
      ),
  })
  .describe('Request body for Adapter creation.');

/**
 * @summary List Adapters
 */
export const ModelsListAdaptersParams = zod.object({
  workspace: zod.string(),
});

export const modelsListAdaptersQueryPageDefault = 1;
export const modelsListAdaptersQueryPageSizeDefault = 100;
export const modelsListAdaptersQueryPageSizeMax = 1000;

export const modelsListAdaptersQuerySortDefault = `created_at`;

export const ModelsListAdaptersQueryParams = zod.object({
  page: zod.number().default(modelsListAdaptersQueryPageDefault).describe('Page number.'),
  page_size: zod
    .number()
    .min(1)
    .max(modelsListAdaptersQueryPageSizeMax)
    .default(modelsListAdaptersQueryPageSizeDefault)
    .describe('Page size.'),
  sort: zod
    .string()
    .default(modelsListAdaptersQuerySortDefault)
    .describe(
      'The field to sort by. To sort in decreasing order, use `-` in front of the field name.'
    ),
  filter: zod
    .object({
      name: zod.string().optional().describe('Filter by adapter name.'),
      model: zod
        .string()
        .optional()
        .describe(
          'Filter by parent (base) model entity reference in the form {workspace}\/{model_name}.'
        ),
      description: zod.string().optional().describe('Filter by description.'),
      fileset: zod
        .string()
        .optional()
        .describe('Filter by fileset reference in the form {workspace}\/{fileset_name}.'),
      finetuning_type: zod
        .enum([
          'lora_merged',
          'all_weights',
          'last_layer',
          'top_layers',
          'gradual_unfreezing',
          'bias_only',
          'attention_only',
          'lora',
          'qlora',
          'adalora',
          'dora',
          'lora_plus',
          'prompt_tuning',
          'prefix_tuning',
          'p_tuning',
          'p_tuning_v2',
          'soft_prompt',
          'ppo',
          'dpo',
          'cdpo',
          'ipo',
          'orpo',
          'kto',
          'rrhf',
          'grpo',
        ])
        .describe('Finetuning types.')
        .optional()
        .describe('Filter by fine-tuning \/ PEFT type.'),
      enabled: zod
        .boolean()
        .optional()
        .describe('Filter by whether the adapter is enabled for inference after training.'),
      created_at: zod
        .object({
          $gte: zod
            .string()
            .datetime({})
            .optional()
            .describe('Filter for results greater than or equal to this datetime.'),
          $lte: zod
            .string()
            .datetime({})
            .optional()
            .describe('Filter for results less than or equal to this datetime.'),
        })
        .optional()
        .describe('Filter entities based on creation date.'),
      updated_at: zod
        .object({
          $gte: zod
            .string()
            .datetime({})
            .optional()
            .describe('Filter for results greater than or equal to this datetime.'),
          $lte: zod
            .string()
            .datetime({})
            .optional()
            .describe('Filter for results less than or equal to this datetime.'),
        })
        .optional()
        .describe('Filter entities based on update date.'),
    })
    .optional()
    .describe(
      'Filter adapters by name, model (parent model ref string, stored on the adapter), description, fileset, finetuning_type, enabled, created_at, and updated_at.'
    ),
});

export const modelsListAdaptersResponseDataItemNameMax = 255;

export const modelsListAdaptersResponseDataItemNameRegExp = new RegExp('^[\\w\\-.]+$');
export const modelsListAdaptersResponseDataItemWorkspaceMax = 255;

export const modelsListAdaptersResponseDataItemWorkspaceRegExp = new RegExp('^[\\w\\-.]+$');
export const modelsListAdaptersResponseDataItemDescriptionMax = 1000;

export const modelsListAdaptersResponseDataItemEnabledDefault = true;
export const modelsListAdaptersResponseDataItemModelMax = 127;

export const ModelsListAdaptersResponse = zod.object({
  data: zod.array(
    zod.object({
      name: zod
        .string()
        .max(modelsListAdaptersResponseDataItemNameMax)
        .regex(modelsListAdaptersResponseDataItemNameRegExp)
        .describe(
          'Name of the adapter. Name must be unique in the workspace for all Adapters and match the following regex: Allowed characters: letters (a-z, A-Z), digits (0-9), underscores, hyphens, and dots.'
        ),
      workspace: zod
        .string()
        .max(modelsListAdaptersResponseDataItemWorkspaceMax)
        .regex(modelsListAdaptersResponseDataItemWorkspaceRegExp)
        .describe(
          'Workspace of the adapter. Allowed characters: letters (a-z, A-Z), digits (0-9), underscores, hyphens, and dots.'
        ),
      description: zod
        .string()
        .max(modelsListAdaptersResponseDataItemDescriptionMax)
        .optional()
        .describe('Optional description of the adapter'),
      fileset: zod
        .string()
        .describe(
          'Fileset where the adapter files are stored expected format {workspace}\/{fileset_name}'
        ),
      finetuning_type: zod
        .enum([
          'lora_merged',
          'all_weights',
          'last_layer',
          'top_layers',
          'gradual_unfreezing',
          'bias_only',
          'attention_only',
          'lora',
          'qlora',
          'adalora',
          'dora',
          'lora_plus',
          'prompt_tuning',
          'prefix_tuning',
          'p_tuning',
          'p_tuning_v2',
          'soft_prompt',
          'ppo',
          'dpo',
          'cdpo',
          'ipo',
          'orpo',
          'kto',
          'rrhf',
          'grpo',
        ])
        .describe('Finetuning types.')
        .describe('Type of finetuning (LORA, P_TUNING, etc.)'),
      enabled: zod
        .boolean()
        .default(modelsListAdaptersResponseDataItemEnabledDefault)
        .describe('Whether to make this adapter available for inference post training'),
      lora_config: zod
        .object({
          alpha: zod.number().optional().describe('Alpha scaling used for this adapter'),
          rank: zod.number().describe('LoRA Rank'),
        })
        .optional()
        .describe('Lora configuration specifics'),
      model: zod
        .string()
        .max(modelsListAdaptersResponseDataItemModelMax)
        .optional()
        .describe(
          "Parent model entity reference. A single name (2-63 characters) or 'workspace\/model_name' where each segment is a valid name (lowercase, digits, hyphens, and temporarily @ . + _; no leading\/trailing or consecutive hyphens). If one slash, both sides must be non-empty."
        ),
      created_at: zod.string().datetime({}).optional(),
      updated_at: zod.string().datetime({}).optional(),
    })
  ),
  pagination: zod
    .object({
      page: zod.number().describe('The current page number.'),
      page_size: zod.number().describe('The page size used for the query.'),
      current_page_size: zod.number().describe('The size for the current page.'),
      total_pages: zod.number().describe('The total number of pages.'),
      total_results: zod.number().describe('The total number of results.'),
    })
    .optional()
    .describe('Pagination information.'),
  sort: zod.string().optional().describe('The field on which the results are sorted.'),
  filter: zod.record(zod.string(), zod.unknown()).optional().describe('Filtering information.'),
});

/**
 * @summary Get Adapter
 */
export const ModelsGetAdapterParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

export const modelsGetAdapterResponseNameMax = 255;

export const modelsGetAdapterResponseNameRegExp = new RegExp('^[\\w\\-.]+$');
export const modelsGetAdapterResponseWorkspaceMax = 255;

export const modelsGetAdapterResponseWorkspaceRegExp = new RegExp('^[\\w\\-.]+$');
export const modelsGetAdapterResponseDescriptionMax = 1000;

export const modelsGetAdapterResponseEnabledDefault = true;
export const modelsGetAdapterResponseModelMax = 127;

export const ModelsGetAdapterResponse = zod.object({
  name: zod
    .string()
    .max(modelsGetAdapterResponseNameMax)
    .regex(modelsGetAdapterResponseNameRegExp)
    .describe(
      'Name of the adapter. Name must be unique in the workspace for all Adapters and match the following regex: Allowed characters: letters (a-z, A-Z), digits (0-9), underscores, hyphens, and dots.'
    ),
  workspace: zod
    .string()
    .max(modelsGetAdapterResponseWorkspaceMax)
    .regex(modelsGetAdapterResponseWorkspaceRegExp)
    .describe(
      'Workspace of the adapter. Allowed characters: letters (a-z, A-Z), digits (0-9), underscores, hyphens, and dots.'
    ),
  description: zod
    .string()
    .max(modelsGetAdapterResponseDescriptionMax)
    .optional()
    .describe('Optional description of the adapter'),
  fileset: zod
    .string()
    .describe(
      'Fileset where the adapter files are stored expected format {workspace}\/{fileset_name}'
    ),
  finetuning_type: zod
    .enum([
      'lora_merged',
      'all_weights',
      'last_layer',
      'top_layers',
      'gradual_unfreezing',
      'bias_only',
      'attention_only',
      'lora',
      'qlora',
      'adalora',
      'dora',
      'lora_plus',
      'prompt_tuning',
      'prefix_tuning',
      'p_tuning',
      'p_tuning_v2',
      'soft_prompt',
      'ppo',
      'dpo',
      'cdpo',
      'ipo',
      'orpo',
      'kto',
      'rrhf',
      'grpo',
    ])
    .describe('Finetuning types.')
    .describe('Type of finetuning (LORA, P_TUNING, etc.)'),
  enabled: zod
    .boolean()
    .default(modelsGetAdapterResponseEnabledDefault)
    .describe('Whether to make this adapter available for inference post training'),
  lora_config: zod
    .object({
      alpha: zod.number().optional().describe('Alpha scaling used for this adapter'),
      rank: zod.number().describe('LoRA Rank'),
    })
    .optional()
    .describe('Lora configuration specifics'),
  model: zod
    .string()
    .max(modelsGetAdapterResponseModelMax)
    .optional()
    .describe(
      "Parent model entity reference. A single name (2-63 characters) or 'workspace\/model_name' where each segment is a valid name (lowercase, digits, hyphens, and temporarily @ . + _; no leading\/trailing or consecutive hyphens). If one slash, both sides must be non-empty."
    ),
  created_at: zod.string().datetime({}).optional(),
  updated_at: zod.string().datetime({}).optional(),
});

/**
 * @summary Delete Adapter
 */
export const ModelsDeleteAdapterParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

/**
 * @summary Update Adapter
 */
export const ModelsUpdateAdapterParams = zod.object({
  workspace: zod.string(),
  name: zod.string(),
});

export const modelsUpdateAdapterBodyDescriptionMax = 1000;

export const ModelsUpdateAdapterBody = zod
  .object({
    description: zod
      .string()
      .max(modelsUpdateAdapterBodyDescriptionMax)
      .optional()
      .describe('Optional description of the adapter'),
    enabled: zod
      .boolean()
      .optional()
      .describe('Whether to make this adapter available for inference post training'),
    fileset: zod.string().optional().describe('Updated fileset for the adapter'),
  })
  .describe('Request model for updating Adapter Sub Entity metadata.');

export const modelsUpdateAdapterResponseNameMax = 255;

export const modelsUpdateAdapterResponseNameRegExp = new RegExp('^[\\w\\-.]+$');
export const modelsUpdateAdapterResponseWorkspaceMax = 255;

export const modelsUpdateAdapterResponseWorkspaceRegExp = new RegExp('^[\\w\\-.]+$');
export const modelsUpdateAdapterResponseDescriptionMax = 1000;

export const modelsUpdateAdapterResponseEnabledDefault = true;
export const modelsUpdateAdapterResponseModelMax = 127;

export const ModelsUpdateAdapterResponse = zod.object({
  name: zod
    .string()
    .max(modelsUpdateAdapterResponseNameMax)
    .regex(modelsUpdateAdapterResponseNameRegExp)
    .describe(
      'Name of the adapter. Name must be unique in the workspace for all Adapters and match the following regex: Allowed characters: letters (a-z, A-Z), digits (0-9), underscores, hyphens, and dots.'
    ),
  workspace: zod
    .string()
    .max(modelsUpdateAdapterResponseWorkspaceMax)
    .regex(modelsUpdateAdapterResponseWorkspaceRegExp)
    .describe(
      'Workspace of the adapter. Allowed characters: letters (a-z, A-Z), digits (0-9), underscores, hyphens, and dots.'
    ),
  description: zod
    .string()
    .max(modelsUpdateAdapterResponseDescriptionMax)
    .optional()
    .describe('Optional description of the adapter'),
  fileset: zod
    .string()
    .describe(
      'Fileset where the adapter files are stored expected format {workspace}\/{fileset_name}'
    ),
  finetuning_type: zod
    .enum([
      'lora_merged',
      'all_weights',
      'last_layer',
      'top_layers',
      'gradual_unfreezing',
      'bias_only',
      'attention_only',
      'lora',
      'qlora',
      'adalora',
      'dora',
      'lora_plus',
      'prompt_tuning',
      'prefix_tuning',
      'p_tuning',
      'p_tuning_v2',
      'soft_prompt',
      'ppo',
      'dpo',
      'cdpo',
      'ipo',
      'orpo',
      'kto',
      'rrhf',
      'grpo',
    ])
    .describe('Finetuning types.')
    .describe('Type of finetuning (LORA, P_TUNING, etc.)'),
  enabled: zod
    .boolean()
    .default(modelsUpdateAdapterResponseEnabledDefault)
    .describe('Whether to make this adapter available for inference post training'),
  lora_config: zod
    .object({
      alpha: zod.number().optional().describe('Alpha scaling used for this adapter'),
      rank: zod.number().describe('LoRA Rank'),
    })
    .optional()
    .describe('Lora configuration specifics'),
  model: zod
    .string()
    .max(modelsUpdateAdapterResponseModelMax)
    .optional()
    .describe(
      "Parent model entity reference. A single name (2-63 characters) or 'workspace\/model_name' where each segment is a valid name (lowercase, digits, hyphens, and temporarily @ . + _; no leading\/trailing or consecutive hyphens). If one slash, both sides must be non-empty."
    ),
  created_at: zod.string().datetime({}).optional(),
  updated_at: zod.string().datetime({}).optional(),
});
